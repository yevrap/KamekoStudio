# Skazka Trail Backlog

> Source of truth for what to build next. See [Skazka Trail — Jam Brief (Vasilisa Pilot)](plans/jam-brief-vasilisa-pilot.md) for the full pilot spec, [Skazka Trail — Concept & Directions](plans/concept-and-directions.md) and [Skazka Trail Questionnaire — Design Decisions](../../archive/questionnaires/skazka-trail-design-decisions.md) for the design rationale.
> **Inbox** = raw capture. **Backlog** = triaged, agent-shippable. **Shipped** = done, newest first. **Parked** = deliberately not doing now.

> **Status check, 2026-07-22 (later same day):** Three tales now exist. The third-tale session initially held, correctly, on a missing Yev-played verdict for Morozko (the log only had an agent self-check). Yev then said directly he doesn't treat that log as a hard gate and to stop blocking on it — so **The Geese-Swans** was picked and built on that basis (see Shipped below), not on a played "keep." Morozko's own played verdict is still genuinely outstanding (see Questions for Yev) — it just no longer blocks the next tale by Yev's own call. The UI/UX visual pass (unparked item below) is a separate, parallel thread.

## Inbox

- **"Back to story select from story. Not just restart. I can see the back button working but I want something on screen too."** *(moved here from an orphaned raw capture at the top of this file, 2026-07-29 morning-brief — not yet root-caused.)* Reads as: the browser Back button already returns to story select (per the Morozko/Geese-Swans ship notes), but there's no in-game/on-screen affordance to do the same — wants a visible control, not just reliance on browser Back.
- **"I also see some options highlighted. Sometimes the only option is continue and sometimes it's highlighted and sometimes it's not. What's going on here? I want the ui to be good and consistent."** *(moved here from the same orphaned capture, 2026-07-29 morning-brief — not yet root-caused.)* Likely relates to the resolution-node "exactly one visible Continue" pattern described in the Shipped log below — worth checking whether the highlight styling is actually inconsistent or just reads that way because the number of visible options varies by node.
- Also see [Improvements](ideas.md)'s inbox — a related raw note ("the player cannot go back in the story") may be the same complaint as the first item above.

## Backlog

-

## Shipped

- ✅ 2026-07-22 — **UI/UX visual pass (mobile + laptop)** shipped — per-tale accent colors (Q1=C/Q6=B), widened desktop column + background treatment (Q2=D), card-grid tale-select (Q3=B), cross-fade + button feedback motion (Q4=C), display face for titles/endings only (Q5=B), light-touch debug-view styling (Q7=B). Answers in [Skazka Trail Questionnaire — Visual Design](../../archive/questionnaires/skazka-trail-visual-design.md) (now archived — `docs/archive/`). Supersedes the Parked "Visual bar upgrade" gate below.
- ✅ 2026-07-22 — Jammed **The Geese-Swans** as the third tale to `drafts/skazka-trail/` (`a7f36e9`) — picked fresh per Q9=C on Yev's explicit go-ahead (not gated on a played Morozko verdict — see status note above). Weighed like Morozko: cheapest of the remaining candidates (Geese-Swans / Ivan Tsarevich / stretch Sadko, Marya Morevna), and the first to exercise a flag set early silently changing the *outcome* of an identical-looking choice much later, rather than gating a new option's visibility (Vasilisa) or driving a single accumulated total (Morozko). `engine.js` reused unmodified. `tales/geese-swans.js`: 15 nodes, three independent booleans (`kindStove`/`kindTree`/`kindRiver`) set refusing or accepting the stove/tree/river on the way out; the return leg doesn't re-branch the graph, each node's flavor text keys off its matching flag (same pattern as Vasilisa's `dollHelp`); `resolution` sums the three flags into one of 3 endings via mutually-exclusive `when` guards (same mechanism Morozko's `resolution` already uses). 3 endings: "The Debts Repaid" (3/3), "A Near Thing" (1–2/3, game-original middle path), "Lost to the Geese" (0/3, canonical dark ending, kept real per Q6). Verified in-browser: all 3 endings reached via distinct answer combinations, all six per-flag flavor-text variants confirmed correct, all three tales reachable from select, Vasilisa (17 nodes) and Morozko (9 nodes) confirmed unchanged via debug dump, `📜 Story so far` confirmed working, zero console errors, phone + desktop + light/dark all checked. Deployed and live (Pages run green). Logged in [Kameko Playtest Log](../../playtest-log.md) for Yev's verdict.
- ✅ 2026-07-22 — Jammed **Morozko** as the second tale to `drafts/skazka-trail/` (`71b917f`) — `engine.js` reused unmodified; `tales/morozko.js` content pack, 9-node scene graph, a single accumulated `patience` flag (0–3) across three rising-cold "Are you warm, maiden?" trials, ending selection via mutually-exclusive `when`-guarded choices at a `resolution` node (same conditional-visibility mechanism as Vasilisa's forbidden-question gate — no engine change needed). 3 endings: "The Gift" (3/3), "Mercy" (1–2/3, game-original middle path, flagged as such in the file header), "The Silence" (0/3, canonical dark ending, kept real per Q6). Added a bare-text tale-select screen to `index.html` (`?tale=<id>` links, `?debug=1`/story-so-far work per active tale, browser Back returns to select). Verified in-browser: all 3 endings reached via distinct answer combinations, resolution node confirmed to show exactly one gated "Continue" in every case, both tales reachable from select, Vasilisa confirmed unchanged (identical debug dump), zero console errors, phone + desktop + light/dark all checked. Deployed and live. Logged in [Kameko Playtest Log](../../playtest-log.md) for Yev's verdict.
- ✅ 2026-07-22 — Jammed the Vasilisa the Beautiful pilot to `drafts/skazka-trail/` (`a9a7140`) — reusable `engine.js` + `tales/vasilisa.js` content pack, 17-node scene graph, flag-based rule-of-three branching (three chores × alone/doll, three Riders × watch/hurry gating a later forbidden question), 3 distinct endings, debug graph view (`?debug=1`, with a structural lint) + in-fiction "story so far" page, bare text, English only, restart button. Verified in-browser: all 3 endings reached and confirmed correct, both `qanda_final` flavor-text variants confirmed, curiosity-gating confirmed on both a triggering and non-triggering run, zero console errors, phone + desktop + light/dark all checked. Deployed and live. Logged in [Kameko Playtest Log](../../playtest-log.md) for Yev's verdict.

## Parked

- **Fourth+ tale for the anthology** — pick fresh once there's a played verdict on the anthology so far (Q9=C — no pre-planned order); remaining candidates are Ivan Tsarevich and the Grey Wolf, or stretch picks Sadko / Marya Morevna.
- **Mirrored second playthrough** (a contrasting run through the same tale, e.g. an unkind version of Vasilisa) — deferred to iteration 2 per Q11=B, gated on this verdict.
- ~~**Visual bar upgrade**~~ — unparked 2026-07-22, moved to Inbox above as a UI/UX pass; Yev chose to work on it now rather than waiting on a keep verdict.
- **Russian localization** — Q8 chose English-only for the pilot; RU can follow once the format's proven (same path as durak's localization).
- **`localStorage` "endings seen" counter** — cheap replay hook flagged in the concept doc's Mechanics section; not in the Modest-bar scope for this jam, worth adding once the format has a keep verdict.
- **Illustration / sound** — explicitly post-verdict per the concept doc's own MVP-slice framing.

## Questions for Yev

- Q1–Q12 are all answered in [Skazka Trail Questionnaire — Design Decisions](../../archive/questionnaires/skazka-trail-design-decisions.md). Still outstanding: your own played verdict on **both** Morozko and The Geese-Swans (keep/meh/kill each) — the log currently only has agent self-checks for both, explicitly flagged as not real promotion calls. Per your own steer, this doesn't block picking a fourth tale if you want one before playing these — just say so.
