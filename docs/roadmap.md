# Kameko Studio — Roadmap

Engineering director: Yevster  
Last updated: 2026-07-15

AI agents read this file to pick the next work item. Items marked ✅ are complete. Items marked 🚧 are in progress. All other items are open.

Priorities: P0 (foundation/blockers) → P1 (high-impact polish) → P2 (medium features) → P3 (new games) → Backlog (tech debt).

> **Ship + triage (2026-07-15) — Astro Salon Sprint 2 SHIPPED; two new inbox items handled.** Yev's follow-up questionnaire answers changed Sprint 2 scope before build: Q3 = "no streak after all" (streak cut from p2-33), Q2 = "go bigger — all 12 houses from day one" (p2-34). Both shipped: **p2-33** ✅ year theme v1 + pools 3× + deterministic end-screen fortune tie-in; **p2-34** ✅ Chart Reading room (4 returning guests × rising + 3 whole-sign house questions; every house dealt once per session; design note in the vault Astro Salon folder). Inbox: "move blob zapper to lab" → **p1-41** ✅ shipped same pass (p1-31 controls rework stays open but Lab-tier now); "expert mode / more difficulty modes" → **p2-35**, gated on the new Q4 in the vault `Astro Salon Questionnaire — Promotion Decisions.md` (real product fork — see options there).
>
> **Triage (2026-07-14, night) — Astro Salon round-3 verdict: KEEP; promotion + depth sprints planned.** Yev's round-3 questionnaire (archived in the vault `90-99 Archive/Kameko Arcade/`): Q1=keep → **p1-38** promote per `docs/promotion-checklist.md`; Q2 "no need for text on each slice" → **p1-39** wedge de-clutter (dates off the wedges, hub preview owns ranges); new inbox item "continue shouldn't need scroll" → **p1-40**; Q4=grow the horoscope + Yev's year-of-horoscopes idea → **p2-33** (year theme, bigger pools, streaks, fortune tie-in); Q5=second room → **p2-34** chart-reading session type (design pass first). Q3 confirms read-then-confirm — expert single-tap parked, no row. Sprint order: p1-38 → p1-39/p1-40 riders → Sprint 2 (p2-33, p2-34) only after the module split. Full plan + done-whens: vault `Astro Salon — Promotion & Depth Sprints (July 2026).md`; non-blocking defaults questionnaire: `Astro Salon Questionnaire — Promotion Decisions.md`. **Nothing built yet — planning only.**
>
> **Triage (2026-07-14, evening) — New Game Directions questionnaire ANSWERED and picked up; the P3 jam queue is set.** Yev's picks: **Q1=A** (physics direction jams first) + **Q3=A1** → **p3-06 flow glider is the next jam**; **Q5=C1+C3** (both checked) → p3-07 (pachinko roguelike) + p3-09 (durak-deck score-attack); **Q4=B1+** with light unlocks → p3-08 (one-tower roguelike with hero sortie). Steering folded into `docs/brief.md`, which closes Taste & Tiers Q3: **Q2=B** — pause-proof is a *strong preference, not a house rule*; judge per game, and every jam states how its game pauses. **Q6=C** — look/tech (2D canvas vs Three.js) is a per-game agent judgment call. **Q7=A** — standard loop: jam to the Lab at the Modest bar, playtest verdict decides promotion; no straight-to-gallery builds. Not picked (stay unscheduled in the vault concepts note, deliberately no rows): A2 flick golf, A3 3D marble, B2 lane TD, B3 maze-builder, C2 scratch-card. Questionnaire archived to the vault `90-99 Archive/Kameko Arcade/`; `Kameko Studio — New Game Directions (July 2026).md` stays live as the design source for the four rows.
>
> **Triage (2026-07-14) — vault inbox cleared into new-game planning.** Yev's July 14 inbox items (fast physics game, TD roguelike, Balatro-style mechanic blend, back-to-home reachability) → vault note `Kameko Studio — New Game Directions (July 2026).md` (nine concepts, A1–A3/B1–B3/C1–C3) + open questionnaire `Kameko Studio Questionnaire — New Game Directions.md`. When answered it also closes Taste & Tiers Q3 — update `docs/brief.md`'s TBD genre directions and add P3 rows for the winners (none added yet, deliberately). The one non-gated item landed as **p1-37** (back-to-home without scrolling).
>
> **Triage (2026-07-12, two passes) — inboxes cleared, all questionnaires answered and picked up.** Morning pass: durak + tysiacha vault inboxes → p1-22…p1-27, p2-27…p2-29, b-16…b-18 (each verified against the code); stale p1-12 status fixed. Afternoon pass: Yev answered everything open — Perevodnoy UX Q6=A → p2-13 unblocked (classic W stays); Tysiacha Q8=D → p2-30 (relaxed-bidding setting), Q9=A → p1-28 (free match-end restart); and the **Taste and Tiers questionnaire landed in full** → tiers in the Direction note below, p1-29 (durak-likes to the Lab), p1-30 (delete waterfall), p1-31 (blob-zapper controls), p0-08 unblocked. Consumed questionnaires archived in the vault (`90-99 Archive/Kameko Arcade/`); fresh open-decision questionnaires created per game. Shipped ✅ rows moved to the Completed section at the bottom — the tables now show open work only.
>
> **Triage (2026-07-12, late night) — settings questionnaire FULLY answered; sprint sequence is the ACTIVE plan.** Yev answered all 7 questions of `Kameko Studio Questionnaire — Settings, Automation & Tokens` (now archived in the vault). Headline: **Q4=E (write-in) — remove the token system entirely, everything free.** Full decisions + sprint breakdown: vault note `Kameko Studio — Settings & Automation Cleanup Sprint (July 2026)` (also mirrored in the vault's `Next Best Actions.md`). The plan, in ship order: **Sprint 0 ✅** surface shrink (p1-29/p1-30, `e56cad2`, v15) → **Sprint 1** hygiene (b-19, b-20, rider b-17) → **Sprint 2** Free Arcade / token removal (p1-33, Q4=E; rescopes p1-06 + p2-32 + p2-02) → **Sprint 3** Watch Mode (p1-32, Q1=A+Q2=B+Q3=A; absorbs p2-28) → **Sprint 4** drawer & update polish (p1-34 Q5=B, p1-35 Q6=A, rider b-16). Q7=B: Lab games keep their drawer sections — decided, no work. Agents: pick up the lowest-numbered unshipped sprint.
>
> **Planning (2026-07-12, night) — Settings & Automation Cleanup Sprint scoped.** Yev's verdict: settings are cluttered again — game settings, arcade chrome, update UI, do-nothing tokens, and the freshly-shipped auto modes (p2-22…p2-27) all compete for space. Full audit + sprint plan in the vault: `Kameko Studio — Settings & Automation Cleanup Sprint (July 2026).md`. New rows: **b-19** (settings-key hygiene + guard test) and **b-20** (river-run drawer lifecycle) are unambiguous, ship now; **p1-32** (unified automation UX), **p1-33** (token economy direction), **p1-34** (update-check UX) are blocked on the vault questionnaire `Kameko Studio Questionnaire — Settings, Automation & Tokens.md`. Sequencing: do p1-29/p1-30 (Lab demotion, waterfall delete) before p1-32 so unification touches 6 games, not 10; p2-28's speed/reveal scope folds into p1-32 when it unblocks.
>
> **Triage (2026-07-12, evening) — both fresh open-decision questionnaires answered same day.** Tysiacha: Q1 confirms p2-30's all-pass default as-is; Q2=A → p2-15 (Scripted First Deal) is the priority teaching item, with added scope (a tutorial setting that expands the in-game rules explanations) — p2-16 deprioritized behind it; Q3=A → **p2-31** (new): build the cautious-Vera/aggressive-Boris AI personalities cut from p2-18; Q4=A → p3-05 (Hot-Seat) confirmed "build it," not just keep-on-roadmap. Durak: Q1=A → p2-07 confirmed (build AI personalities/tells, difficulty alone isn't enough); Q2=A → **p2-32** (new): token-spend cosmetics (card backs/table themes) — durak's first real token sink beyond hidden-object's hint cost; Q3=A → p2-12 confirmed "build it soon"; Q4=B → p2-27's open mid-takeover question closed — reveal-hands is a separate toggle, default off. Questionnaires fully answered (no open items left) and archived to `90-99 Archive/Kameko Arcade/`.

> **Direction — tiers are set (2026-07-12).** The verdict-driven loop (vault: `Kameko Studio — Agent Game Loop Roadmap.md`) now has its tiers, from the answered `Kameko Studio Questionnaire — Taste and Tiers.md`:
> **Invest:** durak (the benchmark), tysiacha (keep verdict), keypad-quest, materials-run, river-run, blob-zapper (controls need rework — p1-31), hidden-object. **To the Lab:** durak-alchemist, durak-dungeon, durak-tactics (p1-29 — "mixed mechanics interesting, not worth the main-page spot"). **Delete:** waterfall (p1-30).
> Jam steering: prototype bar = **Modest** (playable + phone-comfortable + restart), cadence = **on-demand only**, what makes durak good = nostalgia/cultural connection + 5–10 min sessions. Jam genre directions (Q3) are still unanswered — the brief (p0-08) should note that. Per-session playtest verdicts land in the vault `Kameko Playtest Log.md` — read it before triaging. Sprint history lives in the Completed section at the bottom and, in full detail, in the vault `Dev Log.md`.

---

## P0 — Foundation

Pre-conditions that should be cleared before adding features.

| ID | Item | Effort | Status | Notes |
|----|------|--------|--------|-------|
| — | *(p0-08 shipped 2026-07-13 → Completed)* | | ✅ | `docs/brief.md` written from the answered Taste & Tiers questionnaire; `/new-game` drops its fallback brief. Q3 (genre directions) + Q7 (free space) remain open in the questionnaire and are flagged as TBD in the brief. |

---

## P1 — High-Impact Polish

Biggest visible improvements to the player experience.

| ID | Item | Effort | Status | Notes |
|----|------|--------|--------|-------|
| p1-38 | Promote astro-salon: draft → arcade game (Sprint 1) | L | ✅ | **Shipped 2026-07-14 (v21).** Full module split (`constants/i18n/content/state/gameplay/ui/main.js` — `content.js` is new: pure text-gen shared by `ui.js` and its tests, keeping `gameplay.js` → `ui.js` one-directional). 24 new unit tests (`tests/astro-salon.test.mjs`): sign data integrity, scoring math, i18n key parity, mulberry32 determinism, content smoke tests. `lastPlayed_astroSalon` + `astroSalon_bestStars` persistence, settings-drawer section, pause via a `state.js` `isPaused()` flag (turn-based game — no timer to freeze), dark/light mode added (the draft had none), pointer events throughout (was `click`). Registered everywhere: root card/GAMES_META/SCORES_META, 3d.html portal, quick game switcher in `shared/settings.js` (was missing — caught in browser verification), both CLAUDE.md files, `clearAllGameData`. Draft deleted. Full test suite + smoke green, verified live in-browser (session play, EN/RU, dark/light, drawer pause, desktop + mobile). |
| p1-39 | Astro Salon: wheel wedge de-clutter (no per-wedge date text) | S | ✅ | **Shipped 2026-07-14 (v21), bundled with p1-38.** Per-wedge date text removed; symbols/names bumped a size (32px/14.5px) to use the freed room. Questionnaire Q1 default (keep names+symbols) applied. Verified in both languages in-browser. |
| p1-40 | Astro Salon: quiz continue button reachable without scrolling | S | ✅ | **Shipped 2026-07-14 (v21), bundled with p1-38.** Continue button moved out of the scrolling `.card` to a fixed `.stage` sibling — the feedback card scrolls internally on tall content, the button never does. Verified in-browser at 375×812 and ~800×450 with a long RU feedback string forcing internal scroll. |
| p1-37 | Arcade chrome: "Back to home" reachable without scrolling | S | open | From the vault Improvements inbox (2026-07-14): getting back to the arcade home currently requires scrolling. Agent first verifies where the back-to-home link sits in each gallery game today, then moves or duplicates it into always-visible chrome (top header or the settings drawer) consistently across games. *Done when:* on a phone-size viewport, every gallery game offers a way back to the arcade home without any scrolling; verified in browser. |
| p1-05 | River Run: ghost run | M | open | Record best run's boat positions in localStorage, replay as transparent ghost on next attempt |
| p1-06 | Hidden Object: hint system (free) | S | open | Hint button highlights the quadrant containing the target. **Rescoped 2026-07-12 (Q4=E, tokens removed):** no token cost — free, limited to ~2 hints per round so it stays a game and not a walkthrough (limit is the agent's judgment call, note it when shipping). Do after Sprint 2. |
| p1-22 | Durak: tap-to-target defense | M | open | From the vault questionnaire `Durak Questionnaire — Perevodnoy UX.md` **Q1=B** (answered; picked up 2026-07-12). After a transfer, 2–3 attacks are open and each tapped defense auto-covers the first open attack it can beat; Yev wants explicit pairing: tap a card, then tap the attack it should cover. Keep the fast path — when only one attack is open (the normal non-transfer case) a tap should still play instantly. *Done when:* with 2+ open attacks, tapping a legal defense highlights the attacks it can cover and the card lands on the tapped one; single-open-attack behavior unchanged; pairing covered by unit tests. |
| p1-23 | Durak: inline transfer/beat choice (replace modal) | S | open | From the vault questionnaire `Durak Questionnaire — Perevodnoy UX.md` **Q2=C** (answered; picked up 2026-07-12). The ⇄ Transfer / 🛡 Beat popup (p1-21) is a centered modal covering the table; Yev wants small inline Transfer/Beat buttons above the hand instead. Same trigger rule (only when a card is legal as both); tapping elsewhere cancels. Coordinate with p1-22 — both touch the defense tap handler. *Done when:* dual-purpose card tap shows the inline buttons (no modal), single-purpose cards play instantly, cancel works; verified in browser. |
| p1-25 | Durak: coach lines in the 📜 log — distinct color + hide toggle | S | open | From the vault durak inbox. Coach hints are logged as `coach_hint` events whenever coach is on (`ui.js` L405) and render like ordinary lines. Style them distinctly (tysiacha's italic-teal hint styling is the in-repo prior art) and add a "show hints" filter toggle inside the logbook drawer so the log can be read as pure game events. *Done when:* coach lines are visually distinct, the toggle hides/shows them live and persists, verified in browser. |
| p1-26 | Tysiacha: turn/phase + contract HUD | M | open | From the vault tysiacha inbox (two items merged: "indicator that it's your turn and what I need to do" + "show the bet you need to beat"). Verified: the only cues today are the transient banner and per-seat `bidLabel` chips that render only during bidding/talon phases (`ui.js` L261); nothing persistent says whose turn it is, what action is expected, or what number is live. Add a persistent status chip: phase + expected action ("Your bid", "Give 2 cards", "Your lead"), current high bid + bidder during the auction, declarer's contract during play. Full en/ru via i18n.js. *Done when:* at any point mid-deal a screenshot answers "whose turn, doing what, against what number"; verified in both languages. |
| p1-27 | Tysiacha: more sounds + phone-speaker pass | S | open | Confirmed by the vault questionnaire `Tysiacha Questionnaire — Language, Difficulty & Sound.md` **Q6** (answered; picked up 2026-07-12): trick-sweep whoosh, match-win fanfare, your-turn nudge — each ~5 lines in `sfx.js`. Fold in the inbox note to tune levels on a real phone speaker (current levels tuned on desktop). *Done when:* three new sounds fire at the right moments, respect the mute toggle, and levels are sanity-checked on mobile. |
| p1-28 | Tysiacha: match-end "New match" goes through the setup screen, free | S | open | Questionnaire **Q9=A** (2026-07-12): the match-end "New match" button (`sum-next` in `main.js`) still charges 1 🪙 while the New Match setup screen restart is free — inconsistent and dodgeable. Route the match-end button to the setup screen like every other restart; no token charge. *Done when:* finishing a match and starting a new one costs 0 tokens and lands on the setup screen; verified in browser. |
| p1-36 | Browser e2e tests: core use cases for all gallery games | M | open | From the vault Improvements inbox ("Ui end to end tests to test core use cases of games", 2026-07-12). The harness now exists: `npm run e2e` (`scripts/e2e.mjs`, puppeteer-core — shipped with the watch-mode fixes, 10 tests covering Watch Mode start/stop/take-over, Clear All Game Data, Lab free-play). Extend it to one core-flow test per gallery game: start a game, perform the basic interaction (play a card / answer a prompt / move the boat / tap a cell), reach or simulate game over, reopen the settings drawer. Keep tests behavior-level (no pixel assertions) so they survive visual polish. State probes: dynamic-`import()` the game's `state.js` for module games; top-level `let` bindings for inline-script games (documented in CLAUDE.md Testing). *Done when:* every gallery game has at least one passing core-flow e2e test and the suite is listed alongside smoke in the definition of done for ships touching game flows. |
| p1-31 | Blob Zapper: rework the control scheme | M | open | **Game moved to the Lab 2026-07-15 (p1-41, Yev's call) — still worth doing, but Lab-tier priority now; a control rework is the natural promotion path back.** Taste questionnaire (2026-07-12), Q1 note: "should be something other than push to move. the graphics are cool but the game needs work." Invest verdict with an explicit gripe about the push interaction. Agent proposes a better control (e.g. direct drag/flick of blobs, gravity-well hold, draw-a-wall) — prototype 1–2 in-game behind a setting if cheap, pick the best feel, make it the default. Yev playtests and verdicts. *Done when:* a new default control ships that doesn't rely on push-to-move, old behavior removed or demoted to a setting, verified on a phone-size viewport. |
| p1-41 | Move Blob Zapper to the Lab | S | ✅ | Yev's inbox (2026-07-15): "move blob zapper to lab." Same treatment as the durak-likes (p1-29): gallery card + quick-switcher entry removed, shelved card added to `drafts/index.html`, docs updated. Game files stay at `games/blob-zapper/`; scores/watch settings untouched. **Shipped 2026-07-15.** |
| p1-32 | Watch Mode — unified automation UX (Sprint 3) | M | ✅ | **Shipped 2026-07-12.** Consolidated Watch buttons to `index.html` across 5 games. Replaced custom settings rows with a shared `KamekoSettings.registerWatchSection()`. Fixed `localStorage` error in node tests. |
| p1-33 | Remove the token system — everything free (Sprint 2, Q4=E) | L | ✅ | **Shipped 2026-07-12.** Removed all token gates, earning logic, and UI elements. Cleaned up `KamekoTokens` from `shared/settings.js`. Cleaned up docs. All games are now completely free to play. |

---

## P2 — Medium Features

New mechanics that add replayability.

| ID | Item | Effort | Status | Notes |
|----|------|--------|--------|-------|
| p2-01 | Arcade Passport (achievement system) | L | open | Cross-game achievements stored in localStorage, passport-stamp UI on index.html dashboard |
| p2-02 | Daily Arcade Challenge | M | open | Date-seeded featured challenge per game on 7-day rotation, shown on dashboard. **Reward framing updated 2026-07-12 (Q4=E):** no token bonuses — reward is streaks/badges (pairs with p2-01's achievement stamps). |
| p2-03 | Keypad Quest: visual wave map | M | open | 3-second interstitial between waves showing upcoming enemy types/counts |
| p2-04 | Keypad Quest: tower upgrades | M | open | Spend in-game currency (kills) on fire rate / splash / range upgrades. Note: `upgradeRandom()` in gameplay.js already applies an automatic random tower level-up every 10 correct answers — that's not player-chosen or currency-spent, so the actual gap (player agency over which upgrade) is still unbuilt. Confirmed during 2026-07-07 roadmap audit. |
| p2-05 | Hidden Object: themed scenes | L | open | Layered scenes (kitchen, forest, space) instead of flat emoji grid |
| p2-06 | Materials Run: material combos | M | open | Cross-material transitions (ice→sand = skid, water→ice = frozen slide) |
| p2-07 | Durak: smarter AI with tells | M | open | Hesitation delays and a "thinking..." indicator already exist (`ai.js` `scheduleAiAction`: 500–900ms random delay; `ui.js` `status-thinking` class). Remaining gap: personality variation per AI seat is still unbuilt. Confirmed during 2026-07-07 roadmap audit. **Confirmed via Q1 of the open-decisions questionnaire (2026-07-12):** "named opponents with distinct styles makes the table feel alive" — build it, difficulty alone isn't enough. One opponent hoards trumps, another attacks recklessly, with hesitation "tells" to read — pairs naturally with tysiacha's p2-31 (same ask, other game). |
| p2-10 | River Run: biome transitions | M | open | Visual storytelling as score increases: forest → canyon → volcanic → space |
| p2-12 | Durak: seeded deals + share link | M | open | Optional seed (URL param + "share this deal" button) producing a deterministic shuffle so a deal can be replayed or sent to a friend. Needs a seeded RNG — durak-dungeon's mulberry32/seedFromString are the in-repo prior art. From the vault durak inbox. **Confirmed via Q3 of the open-decisions questionnaire (2026-07-12):** "I'd use this" — build it soon. |
| p2-13 | Durak: extended stats | S | open | Beyond p2-08's W/L/D: wins as attacker vs defender, take rate, average game length, current streak; small stats view reachable from the game-over overlay or settings. From the vault durak inbox ("Stats"). **Decision made (questionnaire Q6=A, 2026-07-12): keep classic win semantics** — not-the-Durak = W stays as is; extended stats should *additionally* track placement counts (1st/2nd/…/Durak, from `state.finishOrder`) so the placement story is visible without changing what W means. Unblocked. |
| p2-15 | Tysiacha: Scripted First Deal + expanded tutorial | M | open | A stacked tutorial hand that forces a marriage, a trump scenario, and a bid decision to ease new players into the game mechanics. **Confirmed top teaching pick via Q2 (2026-07-12)**: "I want a tutorial setting that expands on the rules" — pair the scripted hand with a tutorial mode that layers extra in-context explanation on top of the existing rules overlay (e.g. inline call-outs at the marriage/trump/bid moments it forces), not just the stacked deal alone. Ship before p2-16. *Done when:* a new player can start a "tutorial" match that deals the forced hand and explains each of the three moments as they happen, toggled off for normal play. |
| p2-16 | Tysiacha: Post-Deal Analysis | S | open | A one-line hint at the end of a deal explaining the biggest missed opportunity or best play made. Deprioritized behind p2-15 per Q2 (2026-07-12) — pick up after the scripted first deal ships. |
| p2-17 | Tysiacha: Match Stats & History | M | open | Track bids made/failed rate, average points per deal, and win streaks. |
| p2-19 | Tysiacha: Seeded Deals & Sharing | M | open | Play a specific deal via a share link (using mulberry32, similar to Durak Dungeon). |
| p2-28 | Tysiacha: spectate upgrades — reveal hands + speed setting | M | merged → p1-32 | **Merged 2026-07-12:** Q1=A/Q2=B make this exactly the tysiacha slice of Watch Mode (Sprint 3). Original scope (Slow/Normal/Fast select replacing binary Fast Forward, "Reveal all hands" toggle, persisted, en/ru) carries into p1-32's spec unchanged — don't implement separately. |
| p2-29 | River Run: cooler auto-mode visualizer | M | open | From the arcade-wide vault inbox: the existing auto mode (Auto-Shoot/Auto-Avoid, inline in `games/river-run/index.html`) just hugs the screen edge — "make it skate around more, not just go to the side." Reconfirmed by the taste questionnaire (2026-07-12): river-run is Invest, "cool visualiser but needs better auto run and maybe other features." Give the auto boat livelier movement: carve lines across the river, weave between obstacles with margin, vary its lane. Consider doing b-01 (module split) first or alongside — the game is still a 1000-line inline monolith. *Done when:* watching auto mode for 60s shows the boat using the full river width with varied, deliberate-looking movement, not edge-hugging; survival not worse than the current bot. |
| p2-30 | Tysiacha: relaxed-bidding setting | M | open | Questionnaire **Q8=D** (2026-07-12): keep the classic forced-100 opener as the default, add a "relaxed bidding" toggle on the New Match screen where the opener is *not* forced — anyone may pass. Needs an all-pass rule: if Raspasy is enabled in Classic Rules, an all-pass deal plays as raspasy; otherwise redeal. AI bidding must respect the mode (easy bots may pass more). Persist with the other match settings (`tysiacha_settings`), full en/ru labels. **Q1 of the open-decisions questionnaire (2026-07-12) confirms this default as-is** — no remaining ambiguity. *Done when:* relaxed mode allows an opening pass, all-pass resolves per the rule above, classic mode unchanged, covered by unit tests. |
| p2-31 | Tysiacha: AI personalities as flavor (cautious Vera vs aggressive Boris) | M | open | Cut from p2-18 when AI difficulty shipped (Easy/Normal/Hard), parked in the vault Improvements inbox since. **Confirmed via Q3 of the open-decisions questionnaire (2026-07-12):** "yes, add personalities." On top of the existing difficulty setting, give each named bot seat a distinct bid threshold and play style (e.g. Vera undervalues risk and protects marriages, Boris overbids and dumps aggressively) — durak's p2-07 is the same ask for the other game, same session is a natural pairing. *Done when:* Vera and Boris are observably different in bidding aggressiveness and card-dumping choices across several deals at the same difficulty level, covered by unit tests on the bid/play thresholds. |
| p2-33 | Astro Salon: year-of-horoscopes + fortune tie-in (Sprint 2) | M | ✅ | Round-3 Q4 (2026-07-14): "I'd actually check it — grow it," plus Yev's inbox idea: "the horoscope should be done for the year… all prewritten… the year can have a theme that can change with each update." Shape: a named **year theme** coloring every read; phrase pools grown well past 10 themes × 8 advice lines; composition stays deterministic per day+sign (mulberry32 over date+sign+theme-version) so the year is effectively prewritten — bumping the version at an update refreshes the whole year. Add a check-in streak (`astroSalon_horoscopeStreak`, visible on the panel) and reuse today's theme in the end-screen fortune so the surfaces agree. Full EN/RU. Do after p1-38. *Done when:* same day+sign always renders the same read, fortune quotes the day's theme, pools ≥3× in both languages. **Shipped 2026-07-15:** streak CUT per questionnaire Q3 ('no streak after all'); `YEAR_THEME` v1 (2026 · The Year of Quiet Momentum) shown on the daily panel and folded into the seed (date+sign+version), pools 30 themes / 24 advice per language (3×+), end-screen fortune quotes the same deterministic read the ✨ panel shows (`dailyReadIndices`, unit-tested). |
| p2-34 | Astro Salon: Chart Reading room — second session type (Sprint 2) | L | ✅ | Round-3 Q5 (2026-07-14): deeper chart reading as a **separate session type chosen at start** (not woven into the 5-guest session). **Design pass first:** short design note in the vault Astro Salon folder answering "what does a 5-minute chart-reading session look like?" — working answer to refine: guests return with birth time/place flavor, place their rising sign, reason about the four angular houses (1st/4th/7th/10th), same 2⭐/1⭐ scoring and rule-on-miss teaching. Real product forks become a questionnaire; otherwise agent judgment, recorded in the note. Then v1: Salon / Chart Reading choice on the start screen, 5 guests, EN/RU, tests on the new pure logic. Ambition check: vault `Astro Salon Questionnaire — Promotion Decisions` Q2. Do after p1-38. *Done when:* design note linked from the vault overview, both session types playable, chart session teaches rising + the houses, unit tests cover the new rules, deploy verified. **Shipped 2026-07-15:** scope upgraded to ALL 12 houses per questionnaire Q2 ('go bigger'). 4 returning guests (undisguised) × [rising placement + 3 house questions]; 12 houses dealt exactly once per session; rising = simplified sunrise rule (sun sign at sunrise, +1 sign / 2 h, whole-sign houses); same 2⭐/1⭐/streak scoring; separate best (`astroSalon_bestStarsChart`); EN/RU; pure logic unit-tested (`risingFor`, `houseSignId`, `buildChartGuests`). Design note: vault `Astro Salon — Chart Reading Room Design.md`. |
| p2-35 | Astro Salon: expert mode / difficulty modes | M | open | Yev's inbox (2026-07-15): "i want more difficulty modes. i don't know anything about astrology so I want to keep building out more features but i want there to be an expert mode that is less obvious and interesting for more advanced players." Real product fork — what "expert" means (stakes: 1-try no-hint scoring; memory: labels fade off the wheel; content: reverse riddles / cusp dates / decans; or a separate Expert Evening session type combining them) is **gated on Q4 of the vault `Astro Salon Questionnaire — Promotion Decisions.md`** (default recorded there). Note round-3 Q3 already rejected single-tap answering — read-then-confirm stays in every mode. *Done when:* the questionnaire answer is picked up, the chosen expert mode is playable and clearly labeled, and reaching it doesn't complicate the beginner path. |
| p2-32 | Durak: cosmetics — card backs / table themes (free picker) | M | open | Cosmetics themselves confirmed wanted (durak questionnaire Q2, 2026-07-12); **the token-sink framing died with Q4=E (tokens removed) — rescoped to a free picker.** Card backs and table themes/felt colors, CSS gradients/patterns only (zero-asset convention), a picker in the settings drawer, equipped choice in localStorage (`durak_equipped_back`/`durak_equipped_theme`). No purchase/unlock model. Do after Sprint 2. *Done when:* at least 2 card backs and 2 table themes selectable, applied live, persist across reload; verified in browser. |

---

## P3 — New Games

| ID | Item | Effort | Status | Notes |
|----|------|--------|--------|-------|
| p3-06 | New game jam — Flow Glider (A1): one-touch dive/soar physics | M | ❌ kill | **Jammed to the Lab 2026-07-14 (v23); verdict same day: KILL** — "uninspired copy of Tiny Wings" (see Kameko Playtest Log + the no-reskins rule now in `docs/brief.md`). Draft stays in `drafts/flow-glider/` unlinked from promotion; do not re-pitch the direction without an original hook. `drafts/flow-glider/` — hold to dive, release to soar; perfect downhill landings chain a streak (fever at ×3) and push the sunset back; night ends the run. 2D canvas (Q6=C call): day→night palette keyframes, banded Tiny Wings hills, parallax silhouettes, motion trail, particles. Pause-proof honored (Q2=B): auto-pause on visibilitychange/blur/pagehide + big ⏸ button. Vault docs: `31 Kameko Arcade/Kameko Arcade/Flow Glider/` (overview, Improvements, verdict questionnaire). Next: Yev playtests, logs verdict in Kameko Playtest Log (Q7=A loop); keep → promotion checklist. Design source: vault `Kameko Studio — New Game Directions (July 2026).md` §A1. |
| p3-07 | New game jam — Pachinko roguelike (C3, Peglin-style) | M | ✅ | **Promoted to arcade 2026-07-14.** Originally shipped to Lab in `09e316b` (v20). Promoted with module split, new items (Ghost Orb, Upgrader), web audio, and tests. |
| p3-08 | New game jam — One-tower roguelike with hero sortie (B1+) | M | open | Questionnaire 2026-07-14, Q4=B1+: single central tower, waves converge from all sides, draft 1-of-3 upgrades between waves — plus a tappable **hero-sortie ability** (the Kingdom Rush itch as an ability, not a repositionable unit; growing it into B2 is a post-verdict decision). Meta-progression: **light unlocks** — new upgrade cards join the draft pool after milestones (not pure Balatro-clean runs). Between-wave drafting is the natural pause point. Design source: vault concepts note §B1. |
| p3-09 | New game jam — Durak-deck score-attack roguelike (C1) | M | open | Questionnaire 2026-07-14, Q5=C1: "nostalgia-as-base is exactly the point." Durak's beat/transfer mechanic as Balatro's poker: clear escalating score quotas, buy folk-flavored modifiers between rounds (a домовой that doubles trump beats, a самовар that brews an extra card slot…). Hard constraint: NOT another durak variant — the durak-likes went to the Lab as genre mashes; this blends at the scoring layer only. Card rendering + deck logic already exist in the repo to borrow. Design source: vault concepts note §C1. |
| p3-10 | New game jam — Black Hole in One (A2 flick golf) | S | ✅ | **Jammed to the Lab 2026-07-14 — verdict pending.** Picked up by Yev's direct request same day (the "new signal" the brief requires for a not-picked direction). `drafts/black-hole-in-one/` — drag-release flick golf where each hole is a pocket planetary system: planet gravity bends every shot, planets double as landable fairways (hop-and-putt), the cup is a black hole with capture spiral; slingshot flybys celebrated; repulsor pulsars from hole 6. Infinite procedural holes, par tracking, total vs par. Post-Flow-Glider originality rule applied: Desert Golfing/Angry Birds used for *turn-based flick* feel only, no lifted signature hooks. Pause-proof by construction (Q2=B): fully at rest between flicks; mid-flight auto-pause on blur/visibilitychange. Vault docs: `31 Kameko Arcade/Kameko Arcade/Black Hole in One/`. Verdict 2026-07-15: **KEEP** → promoted to `games/black-hole-in-one/` same day. Full promotion checklist: module split (constants/state/physics/gameplay/sfx/ui/main — physics+gameplay DOM-free behind injected hooks), 20 unit tests, settings drawer section (how-to, new run, sound toggle), `lastPlayed_blackHoleInOne` + `blackHoleInOne_bestRound` persistence, registered everywhere (gallery card+meta, GAMES_META/SCORES_META, 3d portal, drawer switcher, clear-data keys, both CLAUDE.mds). Questionnaire asks shipped with it: ⛳ 9-hole Round mode with scorecard + best-round record alongside Endless (Q3=C); inviting planet-hops — landing window 17→22, soft-catch bounce, 🪐 HOP toast, hops on the scorecard, bigger guard planet (Q4=B); device parity via fixed 100×170 course letterboxed into any viewport — laptop no longer gets a short easy course (Q5). Aim preview untouched (Q2=A). Draft deleted from `drafts/`. |
| p3-11 | Black Hole in One — orbit-and-flick (BH-4) | M | ✅ | **Shipped 2026-07-15 in `4dfb217` (v27).** Yev's post-promotion stable-orbits pick. A near-circular pass around a planet (near-tangential, near circular speed, hugging the surface) snaps into a **live, station-kept orbit** the player flicks out of; the flick is a **force-push impulse ADDED to current velocity** (0 at rest → tee shots unchanged; the orbital velocity when breaking away → the impulse bends the existing motion). `physics.js` `orbitCapture()` pure + unit-tested; rejects dives, slingshot flybys, and near-stationary grazes so it sits between land / slingshot. `gameplay.js` `beginOrbit`/`stepOrbit`, capture armed + cooldown-gated in `stepFlight`, `launch()` adds the impulse, orbit skimming the cup sinks. `main.js` aim-from-orbit (freeze→aim→flick) + main-loop stepping + resume-on-cancel. `ui.js` honest aim preview (current velocity + impulse) + orbit ring. 7 new unit tests (241 total green), smoke green; in-browser via real modules — orbits reachable on all 8 holes sampled (~28% of tee shots) and the full orbit→aim→flick input path through the actual handlers. **Pause-proofing dropped by Yev's ruling** (auto-pause freeze/resume is enough; live/complicated motion is the goal). Vault: `31 Kameko Arcade/.../Black Hole in One/` — [[Black Hole in One — Stable Orbits Exploration]] spec. Orbit-tuning questionnaire answered all-defaults (frequency/reward/cup all kept) → no follow-up code, archived. |
| p3-12 | Black Hole in One — 🌌 Open World Sprint 1 (vertical slice) | L | ✅ | **Shipped 2026-07-15.** New **🌌 Explore** mode: camera-followed bounded world + free-fling, town respawn + coordinate-seeded sector with fuel pickups and stardust. All Q9 perf guardrails respected. Golf modes + 260 tests green. |
| p3-13 | Black Hole in One — Open World Sprint 2 (richness) | M | ✅ | **Shipped 2026-07-17.** Wave 1: Shop Options Bleed fix ✅ (`185f903`), OW-3 black-hole wormhole → Town → return portal ✅ (`8ad331c`), OW-5 moons & rings (decorative) ✅ (`28a6990`); OW-6 (named regions) parked, not built — Yev dropped it in favor of the star map. Wave 2 redirected same day straight to OW-9 — zoom-out star map + fog-of-war discovery ✅ (`92256eb`); OW-7 (wormhole pairs) and OW-8 (derelict loot) parked, unscoped. Per the Open World build plan. No arc chosen since — see the vault's Black Hole in One Next Arc Questionnaire (July 18, 2026). |
| p3-14 | Black Hole in One — Open World Sprint 3 (town & cosmetics) | M | open | Real town hub; **stardust → comet/trail skins** (cosmetics only, no power creep); beacons + fast-travel list; optional **boost/metered-travel** toggles (default stays unlimited free-fling). Per the Open World build plan. |
| p3-15 | Black Hole in One — Open World Sprint 4 (optional purpose) | M | open | Missions/bounties from town, portal-scroll item, richer discovery rewards — all ignorable. Per the Open World build plan. |
| p3-17 | Black Hole in One — 🐞 fix cold-load FOUC + shared-map slowness | S | ✅ | **Shipped 2026-07-18.** Yev reported occasional flash-of-unstyled hamburger button and slow/flaky shared-map (`?map=`) opens. Root-caused via live Resource Timing profiling on the deployed site (not guesswork): (1) `shared/settings.js` injected its `settings.css` `<link>` only at `DOMContentLoaded`, ~700ms into the load — the hamburger button (whose `position:fixed` lives entirely in that CSS) got appended to `document.body` well before its stylesheet arrived, rendering briefly as a plain unstyled inline button. Fix: kick off the CSS fetch immediately when `settings.js` executes instead of deferring it into `injectUI()` — now starts in the first network round (~90ms) instead of ~700ms. (2) No app bug in `decodeMap()` (editor.js:295, confirmed by hand-decoding a synthetic map — pure sync, no network); the real cause of "shared links are slower" is that this is a bundler-less ES-module site (per the no-build-step constraint) — a cold visitor's browser must fetch `main.js` → its 7 direct imports → `physics.js` (only transitively imported) → the JS-injected CSS as up to 4 serialized round trips, and shared-map links are disproportionately opened by first-time, fully-cold-cache visitors who eat that whole waterfall, unlike Yev's own warm-cache dogfooding. Fix: added `<link rel="modulepreload">` for all 8 game modules in `black-hole-in-one/index.html` so the browser's preload scanner fetches the whole graph in one round trip. No service-worker/cache-control issue found — GH Pages default caching (`max-age=600` via Fastly) confirmed via live headers, not implicated. **Verified:** 337/338 unit tests green + full-site `npm run smoke` green (one `river-run` Tone.js CDN flake reproduced identically with and without the diff — confirmed unrelated); manually round-tripped a synthetic `?map=` link through the local server (decode → boot → settings drawer → dark-mode toggle), zero console errors; before/after Resource Timing confirmed the settings.css race is closed and the module graph now fetches in a single round. Full findings: vault `Black Hole in One/Dev Log.md`. |
| p3-16 | Black Hole in One — 🧲 Orbits & Star Map arc | M | 🚧 in progress | Wave 1 (orbits attract + orbits do things): ORB-1 🧲 Orbit Magnet item ✅ shipped 2026-07-18 (`301effb`, v40) — orbit band captures on any approach, item ON by default, golf + item-OFF Explore untouched. Town Orbit (tee body) and tap-to-land ✅ shipped 2026-07-18. TAP-4 (tap the black hole you're orbiting to warp to Town) ✅ shipped 2026-07-18. ORB-2 (flick-at-planet guaranteed landing) superseded by the TAP redesign, not built as originally written. ORB-4 (stardust rings) ✅ shipped 2026-07-19 (`53784ba`) — Orbit Magnet capture snaps onto a ringed planet's ring so orbiting sweeps every dot; also fixed pickups always rendering green regardless of type. ORB-3 (station orbit refuel trickle + Town refuel) still open. Wave 2: MAP-1 (star-map landmarks) ✅ shipped 2026-07-19 (`368e161`), ahead of the Wave 1 checkpoint at Yev's call; MAP-2/MAP-3 (fast travel, pan/zoom) not started. Per the vault's Orbits & Star Map Build Plan (July 2026). |
| p3-18 | Black Hole in One — 🔨 Map Maker Sprint: MM-6 map size chooser | S | ✅ | **Shipped 2026-07-19.** Reprioritized to top priority ahead of the rest of Golf Mode Catch-Up/Explore — Map Maker had stalled since the stability sprint. MM-6 is the sprint's first item and a prerequisite for MM-16 (editor minimap/overview, not yet built): a size chooser now appears when hitting 🔨 Map Maker from the mode menu, offering **small/golf-scale** (200×200, unchanged) or **large/Explore-scale** (600×600, `constants.js` `MAP_SIZES`). The chosen tier is stored as `world.mapSizeKey` (reset to small for golf/endless on every `startRun`, so those modes are provably untouched) and as `mapData.size` on save/share/import; `encodeMap`/`decodeMap` only emit a size tag for 'large', so pre-sprint share links stay byte-identical and decode to 'small' by default (no silent breakage). Editor default tee/hole placement, Add Planet/Add Pulsar centering, the drag-to-delete out-of-bounds threshold, and both `stepFlight`/`stepOrbit`'s "lost in space" OOB checks now all read the active map's bounds via `mapBounds(world.mapSizeKey)` instead of the hardcoded 200×200 — verified in-browser that a large-map Test Play flight travels to ~600 before triggering OOB (previously would have falsely triggered around 200). Camera-follow (both golf/editor's `ui.camera` and Test Play) already tracked the comet unconditionally, so it needed no change to work at the larger scale. 8 new unit tests (size-tier bounds, encode/decode round-trip for 'large', default-to-small for untagged maps, `startEditor(size)` fresh-map placement at both tiers, out-of-bounds delete threshold scaling, `startRun`/`startCustomMap` size reset/default); full suite 372/372 + `npm run smoke` green. MM-16 (overview mode), MM-11 (play from My Maps), MM-13 (custom-map HUD clarity), MM-18 (glossary), MM-15 (planet size options) remain open per the sprint doc. |
| p3-01 | Signal Tower | L | open | Listen to procedurally generated melody, reproduce on colored pads. Web Audio API + Canvas 2D. Mobile-first. |
| p3-02 | Hex Drift | L | open | Hex-grid territory game vs AI. CSS Grid + DOM. Turn-based strategy — fills a gap in the arcade lineup. |
| p3-03 | Glyph Run | L | open | Endless side-scroller: auto-run, swipe to jump/duck/dash, obstacles are glyphs matching a cognitive prompt. Canvas 2D. |
| p3-05 | Tysiacha: Hot-Seat Mode | L | open | Pass-and-play multiplayer for 2-3 local humans, using durak's pass-device cover pattern. **Confirmed via Q4 of the open-decisions questionnaire (2026-07-12):** "I'd actually play this with family" — build it, not just keep it on the roadmap. Still the biggest item on either game's roadmap (L); best suited to a dedicated session rather than a quick one. |

---

## Backlog — Tech Debt

Lower priority but worth doing when a game is getting other attention.

| ID | Item | Effort | Status | Notes |
|----|------|--------|--------|-------|
| b-01 | ES module split: river-run | L | open | index.html is 1006 lines with all JS inline. Split into constants/state/gameplay/main modules. |
| b-13 | Adopt `AGENTS.md`-canonical context pattern; extend generator to other repos | M | open | Cross-repo dev-infra item, not Kameko-specific — noted here since this repo's Backlog is the active tracker. The Obsidian vault ("Optimistic Staircase") already uses the pattern worth copying: `AGENTS.md` at repo root is canonical shared context, `CLAUDE.md`/`ANTIGRAVITY.md` mirror it, `GEMINI.md` is a thin backward-compat stub. Kameko has b-06's generator (`CLAUDE.md` → `GEMINI.md`) but no `AGENTS.md` yet. `~/Development/Clothes Inventory` has no generator at all — its `CLAUDE.md` and `antigravity.md` are hand-maintained separately and have already diverged (218 vs 262 lines as of 2026-07-08). Plan: add `AGENTS.md` as the canonical file in both repos, generalize `scripts/generate-context-docs.js` to derive `CLAUDE.md`/`GEMINI.md`/`ANTIGRAVITY.md` from it, retire Clothes Inventory's hand-maintained `antigravity.md`. Do this as a deliberate per-repo pass, not a blind regeneration — content should be reviewed line by line. |
| b-26 | Watch section: dead Auto-restart switch in keypad-quest & river-run | S | ✅ | **Shipped 2026-07-13 (`082943a`, v18).** Yev chose "remove it." Added `hasAutoRestart:false` opt-out to `registerWatchSection`; both games pass it, switch is now absent. Dead `keypadQuest_autoRestart`/`riverRun_autoRestart` kept in the clear list for legacy cleanup. e2e still green. |
| b-27 | Token-system residue sweep | S | ✅ | **Shipped 2026-07-13 (`082943a`, v18).** Removed dead 3D "Arcade Tokens" achievement, `#settings-get-token-btn` CSS, empty `updateTokenDisplay` in hidden-object + its call, stale tysiacha comment. Left `durak/main.js spendTokenAndStart()` (live function, cosmetic-only misnomer) → see b-29 follow-up. |
| b-18 | Tysiacha: delete dead `#marry` overlay | S | ✅ | **Shipped 2026-07-13 (`082943a`, v18).** Removed markup, `marry-yes`/`marry-no` handlers, three `setText` localization calls, and the unused `marry.title`/`marry.body` i18n keys (en+ru). Kept `marry.no`/`marry.yes` — both live for the action-bar Declare/Play buttons (the backlog note's claim that `marry.yes` was removable was wrong; verified against `ui.js` before deleting). Overlay confirmed absent from live DOM. |
| b-29 | Rename durak `spendTokenAndStart()` → `startMatch()` | S | open | Cosmetic follow-up from the b-27 sweep (2026-07-13). The function no longer spends tokens (removed in Sprint 2) — it just starts a match. Live and correct, only the name is stale residue; called in 4 places in `games/durak/main.js`. Rename for clarity when durak is next touched. Not urgent — internal name, not user-visible. |

---

## Completed ✅

Shipped and closed items, moved out of the working tables on 2026-07-12. Full ship narratives: vault `Dev Log.md`.

### Sprint history

> **Quick-win batch (2026-07-13) — brief + watch-switch cleanup + durak chip + dead-code sweep: ✅ shipped, deploy-verified live as v18 (`082943a`).** Five small independent items in one pass. **p0-08** — wrote `docs/brief.md` from the answered Taste & Tiers questionnaire; `/new-game` now drops its fallback brief (Q3 genre directions + Q7 still open, flagged in the brief). **b-26** — added `hasAutoRestart:false` opt-out to `registerWatchSection`; keypad-quest and river-run (no discrete game-over to loop) no longer show the dead Auto-restart switch — Yev's call ("remove it"); durak/tysiacha/materials-run/blob-zapper keep it. **p1-24** — durak header trump chip shows suit only (rank is already on the face-up trump card under the deck). **b-18** — deleted the dead `#marry` overlay in tysiacha (markup, handlers, localization calls, unused `marry.title`/`marry.body`); the action bar keeps `marry.no`/`marry.yes` — the backlog note wrongly listed `marry.yes` as removable, but it's live for the Declare button, verified before deleting. **b-27** — token residue sweep: dead 3D "Arcade Tokens" achievement, `#settings-get-token-btn` CSS, empty `updateTokenDisplay` in hidden-object, stale tysiacha comment. Left `durak/main.js spendTokenAndStart()` (live function, cosmetic-only misnomer) as a noted follow-up rather than churn call sites. 170 unit + 13-page smoke + 10 e2e green; all five verified in-browser on the live site.
>
> **Bugfix (2026-07-12, late night) — Watch Mode state sync + Free-Arcade residue (b-28): ✅ verified via new e2e suite.** Yev's playtest found keypad-quest autoplay/take-over/stop broken and river-run with no drawer stop control and no autoplay. Root causes, all one class — *localStorage and live state disconnected*: (1) the shared watch section only wrote localStorage, invisible to games that mirror the flag in memory — `registerWatchSection` now takes `onStart`/`onStop` and both games sync in them; (2) keypad-quest's auto-typist dispatched synthetic `input`/`click` events that hit its own manual-takeover abort — aborts now gate on `event.isTrusted`; (3) river-run registered its drawer sections before `shared/settings.js` loaded (inline classic script runs first), so no section ever appeared — registration deferred to `DOMContentLoaded`; (4) river-run's ▶ Watch button never set the in-memory `autoPlay` var. Riders: "Clear All Game Data" crashed on the removed `KamekoTokens.toast` (local toast now); Lab games' stale "Play 1🪙" labels dropped (gates were already gone); dead `autoShoot`/`autoAvoid` vars removed; CLAUDE.md localStorage table caught up. New `npm run e2e` (`scripts/e2e.mjs`): 10 behavior-level browser tests covering all of the above — the seed of p1-36. |
>
> **Bugfix (2026-07-12) — Tysiacha Watch Mode Takeover & Settings Jump: ✅ completed same day, verified live.** Fixed AI failing to take over when Watch Mode is enabled mid-deal in Tysiacha, and fixed the settings drawer scroll-jump when toggling Watch Speed. Root cause: the settings-closed handler didn't refresh `autoPlay` from localStorage or kickstart the AI for the human seat; the drawer render wiped `innerHTML` without preserving `scrollTop`. Commit `204f9bc`.

> **Bugfix (2026-07-12) — Arcade Watch Mode (b-21): ✅ completed same day, verified live.** Fixed Watch Mode section missing from the settings drawer mid-game. `registerWatchSection` was inappropriately hidden when `autoPlay` was false, preventing players from enabling watch mode during a game. Removed the `when` condition and changed the bottom button to toggle between '▶ Watch' and 'Take Over / Stop' based on the active state. Commit `caa73f7`.

> **Sprint (2026-07-12) — Drawer & update polish (Sprint 4): ✅ completed same day, verified live.** Implemented p1-34 (Update UX deferral), p1-35 (Drawer regroup into Arcade cluster), and b-16 (Drawer scroll affordance). Tests and smoke checks pass. Vault logs updated.

> **Sprint (2026-07-12) — Watch Mode (Sprint 3): ✅ completed same day, verified live.** Implemented p1-32. Unified automation UX to "Watch Mode". Added ▶ Watch buttons to start screens. Shared settings drawer logic in `registerWatchSection`. Fixed `localStorage` error in tysiacha node tests. Smoke tests pass, GitHub Pages deployed successfully. Vault: updated Next Best Actions and Dev Log.
> **Sprint (2026-07-12) — Free Arcade (Sprint 2): ✅ completed same day, verified live.** Implemented p1-33 (Remove the token system). Completely stripped `KamekoTokens` mechanics across all games, UI sections, and settings. Updated documentation (`CLAUDE.md`, `promotion-checklist.md`, `GEMINI.md`). Tests passing, successfully deployed. Vault: updated Next Best Actions and Dev Log.

> **Sprint (2026-07-12) — Settings-key hygiene & drawer fix: ✅ completed same day, verified live.** Implemented b-19 (guard test for localStorage keys + full coverage), b-20 (migrated river-run drawer section to boot lifecycle), and b-17 (favicon 404 fix). Smoke tests pass, GitHub Pages deployed successfully. Vault: updated Next Best Actions and Dev Log.

> **Sprint (2026-07-11, evening) — Durak: post-transfer draw bug: ✅ completed same day, verified live.** Yev's screenshot showed his hand stuck at 5 cards during his own attack turn with the deck non-empty. Shipped p0-15 in `3ee87b4`: `playTransfer` overwrote `state.contributionOrder` instead of adding to it, silently dropping the original attacker from the post-bout draw phase. Verified via unit test, a live-browser drive of the exact scenario through the running page's own modules, and confirmed present in the deployed `gameplay.js` on GitHub Pages. Vault: `31 Kameko Arcade/Kameko Arcade/durak/`.

> **Sprint (2026-07-11, PM) — Durak perevodnoy fixes: ✅ completed same day.** Yev's playtest of the transfer variant (p2-11) found the game hanging after transfers and no way to choose beat over transfer. Shipped p0-14 (deadlock), p1-21 (choice popup), plus the two open vault-inbox items p1-09 (hand sort) and p1-10 (placements). p2-11 had shipped with zero transfer tests — the suite now covers the variant (167 tests). Vault: overview + questionnaire in `31 Kameko Arcade/Kameko Arcade/durak/`.

> **Sprint (2026-07-10) — Settings Drawer Recovery: ✅ completed 2026-07-11.** Yev's playtest of the Drawer API migration (`641732d`) found Tysiacha broken and the drawer UX incoherent. All seven items shipped (p0-12, p0-13, p1-17, p1-18, p1-19, b-14, b-15) plus p1-20 (quick game switcher, from questionnaire Q4). Audit with full findings: vault note `Kameko Studio — Settings Drawer Audit & Sprint (July 2026).md` (archived); the answered questionnaire that steered p1-17/p1-18/p1-19/p1-20: vault `Kameko Studio Questionnaire — Settings & Drawer UX.md` (archived).

### P0 — Foundation

| ID | Item | Effort | Status | Notes |
|----|------|--------|--------|-------|
| p0-01 | Add durak-alchemist to CLAUDE.md and GEMINI.md game tables | S | ✅ | Already done — durak-alchemist has been in the Games table in both files since commit `0eb7f36`, the same commit that created this roadmap and listed the item as open. Found stale during the 2026-07-07 roadmap audit. |
| p0-02 | Delete orphaned game.js monoliths | S | ✅ | Deleted 2026-07-07 in `d42587c`. Verified zero references before removal; also fixed a stale doc pointer in games/CLAUDE.md. |
| p0-03 | Add durak-alchemist unit tests | M | ✅ | Shipped 2026-07-07 in `93be05d`. 28 tests covering gridLogic.js (spawnCard, getEmptyCells, spawnRandomBaseCard, slideGrid, isGameOver) and combatLogic.js (canBeat, canTransfer, getExposedRanks). Also added games/durak-alchemist/package.json (type:module) so Node's test runner can import the game's ES modules, matching games/durak/'s existing pattern. |
| p0-04 | Remove Tailwind CDN from blob-zapper, hidden-object, waterfall | M | ✅ | Shipped 2026-07-07 in `f9955d6`. blob-zapper had zero Tailwind classes (script tag only); waterfall had one heading; hidden-object was fully ported to plain CSS (responsive breakpoints, colors, modals). Caught and fixed a self-introduced cascade-order bug (`.hidden` losing to `.modal-overlay`'s `display:flex`) via in-browser testing before committing. |
| p0-05 | Fix waterfall missing lastPlayed_ write | S | ✅ | Shipped in `0a6fcb3` — `games/waterfall/index.html:223` writes `lastPlayed_waterfall` right after the token spend. Status was already ✅ on disk; note text updated for clarity during the 2026-07-07 /ship pass. |
| p0-06 | Add durak-alchemist to games/CLAUDE.md and games/GEMINI.md | S | ✅ | Shipped 2026-07-07 in `5c889a7`. Added durak-alchemist rows to the ES Module Refactor Status, Games, and localStorage Keys tables in both files. |
| p0-07 | Add unit tests for durak-dungeon and durak-tactics pure logic | M | ✅ | Shipped 2026-07-07 in `63c1879`. durak-dungeon: 20 tests on constants.js + state.js (mulberry32, seedFromString, shuffle, canDefend incl. no-trumps mutation, hasRelic, getActiveTrumpSuit). durak-tactics: 7 tests on constants.js's `getDisplayVal` — its only DOM-free function; the rest of the game's logic lives in gameplay.js, which reads DOM elements at module load and isn't unit-testable without a refactor (documented as an intentional ui+gameplay merge in games/CLAUDE.md). |
| p0-09 | `/new-game` command — jam workflow | M | ✅ | Shipped 2026-07-09. `.claude/commands/new-game.md`: 8-phase jam workflow (steering inputs → 3 pitches → single-file prototype in `drafts/<slug>/` → PLAYTEST.md → Lab registration → browser verify → ship → report). Includes the fallback brief (durak benchmark + "teach unknown rules in-game") since p0-08 is still blocked on the vault questionnaire. Exercised the same session by the tysiacha jam (p3-04). |
| p0-10 | Drafts index page | S | ✅ | Shipped 2026-07-09. `drafts/index.html` ("The Lab") lists prototypes newest-first with name/one-liner/date; linked from the root header as a `Lab 🧪` mode-link next to 3D Mode (more findable on the phone than the settings dev section). `drafts/CLAUDE.md` rewritten to document the Lab + prototype conventions. |
| p0-11 | Promotion checklist doc | S | ✅ | Shipped 2026-07-09. `docs/promotion-checklist.md`: 6-phase draft→game graduation (move & module split, tests, shared-infra hooks, mobile patterns, registration in all indexes/doc tables, verify & ship) with the repo's reference implementations named per step so `/ship promote <slug>` can run it without questions. |
| p0-12 | Tysiacha: fix boot crash + dead settings bindings | S | ✅ | **Shipped 2026-07-11** in `972820c`; pushed alone ahead of the rest of the sprint and verified live (v12 served ~40s after push). Split localization into `localizeStatic()` (index.html chrome) + `localizeDrawer()` (injected drawer content) with null-safe setters. Original diagnosis: The Drawer migration (`641732d`) removed `btn-howto`/`btn-coach`/the old settings modal (incl. `set-title`) from `games/tysiacha/index.html`, but `ui.js` `localizeStatic()` still sets `.textContent` on them → throws `Cannot set properties of null`. Two blast radii: (1) boot — `main.js` calls `localizeStatic()` before `newMatch()`/`showHowto()`, so the game loads to an empty table; (2) drawer — the settings section render calls `localizeStatic()` before binding events, so EVERY control is dead: player-name inputs (regresses p2-21), language select (p1-14), difficulty (p2-18), sound (p1-13), 1-tap, target score, Classic Rules, and Apply & Restart. Fix: split into `localizePage()` (index.html elements only) + `localizeDrawer()` (called from the drawer render), and/or a null-safe `setText(id, txt)` helper so a missing element can never abort boot/bindings. *Done when:* hard reload boots to a dealt hand with zero console errors; renaming a seat updates in-game and survives reload; language/difficulty/sound/1-tap apply live; Apply & Restart starts a match with the chosen target + rules. Verified in-browser 2026-07-10; full audit in the vault note named in the Sprint callout above. |
| p0-14 | Durak: post-transfer deadlock — game hangs after a perevodnoy transfer | M | ✅ | **Shipped 2026-07-11.** From Yev's playtest (vault durak inbox, with screenshot): after a transfer left ≥2 undefended attacks, `playDefense` handed priority back to the attacker after covering just one — the attacker could neither add a card (no rank match) nor pass (`passAttack` requires a fully defended field), so the AI no-op'd and rescheduled forever ("CPU 2 attacking…" + spinner). Fix: priority returns to the attacker only when the field is fully defended, else stays with the defender; `legalDefense` widened so a defense may cover ANY open attack the card beats (was silently rejecting cards that couldn't beat `attacks[firstOpen]` specifically); normal + hard AI defend logic made target-aware (takes when the first open attack has no beater). 11 new tests incl. a deadlock regression; verified live in browser on the exact reported board. |
| p0-15 | Durak: original attacker never draws back up to 6 after a transfer | S | ✅ | **Shipped 2026-07-11, verified live** (`3ee87b4`). From Yev's screenshot (vault durak inbox): hand stuck at 5 cards during his own attack turn with the deck non-empty. Root cause: `playTransfer` (`gameplay.js`) did `state.contributionOrder = [seat]`, overwriting the array instead of adding to it — the seat that threw the original attack (recorded there by `playAttack`) was silently dropped, and `drawPhase`'s "ensure primary attacker draws" fallback doesn't catch them either because `playTransfer` also reassigns `state.attackerSeat` to the transferrer. Net effect: whoever threw the first attack of a transferred bout never draws replacement cards again, for the rest of the game. Same defect applies to chain transfers. The existing test suite had baked in the buggy value (`durak.test.mjs` asserted `contributionOrder === [1]` post-transfer, missing seat 0) — the perevodnoy variant shipped same-day as p0-14/p1-21 with no test actually exercising the draw phase after a transfer. Fix: push instead of overwrite. *Done when:* a full transfer bout, once defended, redraws every seat that contributed a card (original attacker included) back to 6; regression test added; existing suite green. |
| p0-13 | Page-load smoke test: every game boots clean | M | ✅ | **Shipped 2026-07-11** in `45c74a8`. `npm run smoke` (`scripts/smoke.mjs`): local static server + headless system Chrome via `puppeteer-core` (no browser download), fails on any uncaught page error; all 14 pages green; verified it fails when the p0-12 crash is reintroduced. Documented in CLAUDE.md Testing. Original spec: The 149-test headless suite was green while tysiacha was unbootable (tests skip the DOM via `typeof document` guards). Add an automated check that loads each game's `index.html` (all 10 games + root gallery + drafts index) in a real or simulated browser and fails on any uncaught error/unhandled rejection at load. Implementation is the agent's choice (Playwright dev-dep, or jsdom loader), but it must run from `npm test` or a sibling `npm run smoke` documented in CLAUDE.md. *Done when:* reverting the p0-12 fix makes the check fail; with the fix it passes for every page. |

### P1 — High-Impact Polish

| ID | Item | Effort | Status | Notes |
|----|------|--------|--------|-------|
| p1-35 | Drawer regroup: one compact "Arcade" cluster (Sprint 4, Q6=A) | S | ✅ | **Shipped 2026-07-12.** **New 2026-07-12 from Q6=A.** Game sections stay on top; everything arcade-wide below folds into ONE visually distinct compact cluster — quick switcher, theme + Gallery, collapsed ⚙️ App (token row is gone after Sprint 2). A labeled/bordered group ("Arcade") so the game/arcade boundary reads at a glance. Pair with b-16 (scroll affordance + reset-on-open) in the same session. *Done when:* every game's drawer shows game sections then one distinct Arcade cluster, verified in durak + tysiacha + root gallery, light + dark. |
| p1-34 | Update-check UX: defer the banner, keep manual Check, add Simulate Update (Sprint 4, Q5=B) | S | ✅ | **Shipped 2026-07-12.** **Unblocked 2026-07-12: Q5=B.** Keep the ⚙️ App manual Check button and its result box; the AUTO banner must never interrupt active play — on `visibilitychange`/`pageshow` during a live session, hold the banner until game-over/menu/gallery context (games already signal pause via `settingsOpened`; simplest: gate the banner on the game's start/game-over overlays being visible, or defer to next `settingsOpened`). Add the "Simulate Update" Developer Mode button the PWA versioning plan specified but never shipped, so the banner flow is testable without a real deploy. *Done when:* a version bump mid-game shows nothing until the session ends (or menu/drawer opens), the manual Check still works, Simulate Update previews the banner locally. |
| p1-01 | Durak: card animations | M | ✅ | Already implemented — `ui.js` runs a generic FLIP animation on every `renderAll()`, covering plays sliding to the field, pickups fanning into hand on take, and round resolution sweeping to discard. Shipped in `b663dfa`; discovered already-done during 2026-07-07 /ship triage. |
| p1-02 | Token economy: earn tokens by playing | M | ✅ | Shipped 2026-07-08 in `56edd2d`. `window.KamekoTokens.earn(n, reason)` adds tokens and logs to a capped 50-entry `tokenHistory` array in localStorage. Wired into all 9 gallery games with an actual finish/best hook (finish=1, new personal best=2); `waterfall` excluded — it has no game-over or score-persistence concept at all, so there was nothing to hook. `durak`/`hidden-object`/`blob-zapper` get a flat 1 token (no comparable "high score" concept). Settings-panel faucet (`add()`) untouched as fallback. Discovered and left alone (out of scope) a pre-existing bug: `durak-alchemist/index.html` loads `settings.js` before `<body>` exists, so `applyTheme()` throws on load and the gear/settings overlay never renders there — `window.KamekoTokens` itself is still defined before that throw, so token earning is unaffected, but the settings UI is broken on that page independent of this ticket. Worth a follow-up backlog item. |
| p1-03 | Blob Zapper: juice + feedback | S | ✅ | Shipped 2026-07-08 in `ec6bb51`. Zapping a blob (touching the destruction zone) spawns a small particle burst and kicks a decaying screen shake. Note: this game has no "escape" mechanic (blobs are eliminated by touching the zone, not by fleeing it), so shake triggers on zap instead. The destruction zone doesn't literally shrink either — reinterpreted "pulsing color when destruction zone shrinks" as the zone's pulse amplitude/stroke intensity rising as remaining blob count drops toward zero. Verified visually in browser (screenshot + console check), no unit tests apply (pure Canvas game). |
| p1-04 | Blob Zapper: combo system | S | ✅ | Shipped 2026-07-08 in `e0ced4f`. Zaps within a 700ms window escalate a x1–x5 score multiplier with a "xN COMBO!" popup. The game had zero score concept before this, so a `score` + persisted `blobZapperHighScore` were added as the necessary base for "multiplier" to mean anything; beating the high score now awards 2 tokens (was flat 1), so blob-zapper was removed from the "no comparable best" list in CLAUDE.md/GEMINI.md. |
| p1-07 | Fix AI Hard failing test (durak ai.js bug) | M | ✅ | Shipped 2026-07-08 in `9c9a629`. Root cause: `aiPlayHard`'s defend branch forced a trump defense whenever the deck was empty, before the existing comfort-based take/defend check could run. Now the forced-defend path only fires when defending would clear (or nearly clear) the hand (genuine survival), otherwise the comfort check can declare Take. Also fixed the test's fixture — the attacker seat needs a matching-rank card so `declareTake` settles at `pileOn` instead of auto-resolving via `endBout`. All 113 tests pass. |
| p1-08 | High-score displays: celebrate new records | S | ✅ | Shipped 2026-07-08 in `afe7df4`. Added a gold pulse/badge treatment: materials-run gets a pulsing "New Record!" badge on the top-score line matching the mode just played; durak-alchemist's game-over screen previously showed no final score or record indicator at all, both added; blob-zapper's existing "New Best!" text now scale-pulses; keypad-quest's new-best wave-clear message renders gold instead of the same cyan as a normal clear. Also fixed a pre-existing keypad-quest bug found during verification: `endWave()` pushed the float text then immediately called `startWave()`, which reset `state.floats` in the same synchronous call before any frame rendered it — the wave-clear/new-best message never actually displayed for either case. river-run left out of scope: it has no score display on its game-over overlay at all today, a larger gap than "no visual distinction." |
| p1-09 | Durak: hand sort toggle | S | ✅ | **Shipped 2026-07-11.** Off/Suit/Strength 3-way toggle in the drawer's Current Match section (below AI Difficulty), instant-apply, persists as `durak_sort` (added to `clearAllGameData`). Display-order only — `sortedHandForDisplay()` in ui.js sorts a copy; the hand array, tap-to-play, and FLIP animations are untouched. Strength = weakest→strongest with trumps last (`cardStrength`). Original spec: Sort my hand by suit-then-rank or by strength (trump last); persist the choice in localStorage. From the vault durak inbox. |
| p1-10 | Durak: finishing-placement display | S | ✅ | **Shipped 2026-07-11.** `state.finishOrder` records seats as `eliminationSweep` retires them (durak appended last by `checkGameOver`); game-over overlay renders a placement list (1st/2nd/… rows, your row gold-bordered, durak row red "Durak" label) — only when every seat has a recorded finish, so mid-game "End round" shows nothing. Known simplification: seats emptying in the same sweep tie in seat order. Original spec: Game-over overlay shows the finish order for all seats, not just who is the durak. From the vault durak inbox. |
| p1-11 | Tysiacha: UI/UX Overlay Improvements | S | ✅ | Shipped 2026-07-09. Upgraded overlays to have fixed headers and close buttons, with scrollable bodies, and tap-outside-to-close behavior. |
| p1-12 | Tysiacha: Card animations | M | ✅ | Shipped 2026-07-10 in `efa8cde` (FLIP slide-to-trick + sweep-to-winner, 1-tap play, selection spacing), follow-up polish in `0991e15` (select-to-act setting, spring easing). Status was stale in this table until the 2026-07-12 triage — the ship is recorded in the vault Dev Log and Tysiacha Improvements. Note: `efa8cde` also caused the AI-freeze hotfix (`865e0a4`, deleted import) same day. |
| p1-13 | Tysiacha: Audio layer | S | ✅ | Shipped 2026-07-10 (commit `a460e8b`, sprint slice 3/3). New `games/tysiacha/sfx.js`: synthesized WebAudio, zero assets — filtered-noise **snap** on every card play, two-note bell **chime** on a declared marriage, low **gavel** knock when the bidding is won. AudioContext created lazily after the first user gesture (no autoplay warnings); every entry point no-ops headless and when muted. "Sound effects" checkbox in the game's ⚙️ settings applies live and persists as `tysiacha_muted` (default on). 156 tests green. |
| p1-14 | Tysiacha: Russian Language Toggle | M | ✅ | Shipped 2026-07-10 (commit `d0dc684`, sprint slice 1/3). New `games/tysiacha/i18n.js`: full en/ru string table behind `t(key, …args)` — event stream, coach hints, rules overlay, settings labels, scoring notes, action buttons, static chrome, and Russian rank letters (Т В Д К) on the rendered cards. Log entries are typed (never stored as text), so switching language re-renders the entire match history — verified both directions in browser. `state.bidLabel` converted from display strings to typed `{kind, amount}`. Language select in the game's ⚙️ settings applies **live, mid-deal, no restart** (per the Q2 coach-toggle principle), persists as `tysiacha_lang`. Known nuance: coach hints in the log are snapshots of what coach said at that moment, so pre-switch hints keep their original language. 4 new tests (153 green at the time). |
| p1-15 | Tysiacha: event stream + ambient trick clarity | M | ✅ | Shipped 2026-07-09. Sprint slice 1/3 of the game-log design (vault: `31 Kameko Arcade/Kameko Arcade/Tysiacha/Tysiacha — Game Log & Clarity Design.md`). New `games/tysiacha/log.js`: `logEvent` appends typed entries (stamped with deal+trick) to match-long `state.log`; `eventText` formats every entry; `trickReason` computes why a trick was won. All gameplay/ai banner call sites now route through `announce()` (log + banner as one), so the banner is a view of the log; bids/passes/plays/deal-scores log silently. Live-UI: "led" badge on the leader's trick slot, trump chip gains a led-suit column during a trick (Q8), trick banner explains the win ("+13 — highest ♦" / "trumped with 9♠"), winning card pulses during the 1.25s resolve pause (pacing unchanged — Q11). ui.js render/banner/flashTip got `typeof document` guards so gameplay runs headless in Node. 6 new tests (146 repo-wide green): logEvent stamping, trickReason, eventText grammar, scripted trick replay, marriage logging. Verified in browser: led A♦→"♦ led · — no trump" chip + led badge; Vera off-suit discard trick showed pulse on the winning 10♦ and reasoned banner. |
| p1-16 | Tysiacha: 📜 log drawer | M | ✅ | Shipped 2026-07-09. Sprint slice 2/3 (after p1-15). 📜 button (top-left of the table, mirroring the trump chip) opens a `.overlay/.panel` drawer; global overlay click handler gives close-X + tap-outside for free. `renderLog()` in ui.js: whole-match feed grouped by deal in `<details>` (current deal open, older collapsed — Q9), "Trick N" headers at each lead, per-type line styling (milestones accent, marriages gold, trick results bold, plays/bids muted, hints italic teal), ♥/♦ colored red via `colorSuits()`. Coach hints now ALWAYS logged (Q10): gameplay's `logHumanHint()` captures `coachText()` at every human decision point (bid turn, exchange, play turn) regardless of the hint-bar toggle, which controls live display only. Drawer refreshes on every render while open (game continues underneath). 3 new tests (149 repo-wide green): hint eventText, hint logged on human turn with coach off, no hint on AI turns. Verified in browser: full bidding→marriage→trick sequence readable, live updates while open, tap-outside closes, zero console errors. |
| p1-17 | Tysiacha: settings drawer restructure | M | ✅ | **Shipped 2026-07-11** in `aed9c3f` per questionnaire Q1=A/Q2=A: static New Match `#setup` overlay owns names + target + classic rules (boot lands there; first visit shows rules first); Play is labeled `▶ Play · 1 🪙` when it charges and a settings-triggered restart mid-session is free (verified at 0 tokens); ↺ now opens the setup instead of silently spending; drawer keeps quick actions + instant persisted settings; match settings persist as `tysiacha_settings` JSON; full en/ru i18n. Original spec: Today the drawer is one flat list mixing three apply models: instant (language/difficulty/sound/1-tap), on-change (names), and Apply-gated (target + Classic Rules), with the Apply button styled as *danger* and silently spending a token. Restructure into two visually distinct groups: **Settings** (instant, persisted) and **New Match** (names per Q1, target score, Classic Rules) behind one primary-styled button that states its cost per Q2 (e.g. "Start new match · 1 🪙"). Persist target score + Classic Rules to localStorage (today they reset on every reload) and read them at boot. *Done when:* every control persists across reload; the two groups are visually separated with the apply model stated; the restart button is primary-styled and cost-labeled (or free, per Q2); verified in browser. |
| p1-18 | Durak: context-aware drawer | S | ✅ | **Shipped 2026-07-11** in `a49c8d9`. Match section renders only during an active round (`when()` on state.phase); perevodnoy + first-turn-transfer toggles moved to the start screen's new Rules block (they're read at game init); coach hints became a Quick Action button per Q5=B. Original spec: The drawer shows "Current Match: vs Computer (2 players)", match-rule toggles, and a red "End round & back to menu" button even when the player is already ON the start menu — state leak, confusing. Render the match section only during an active round; on the menu show either nothing game-specific or the Quick Actions alone. *Done when:* drawer opened from the start menu has no Current Match section and no End-round button; opened mid-round it has both; verified in browser. |
| p1-19 | Shared drawer chrome de-clutter | S | ✅ | **Shipped 2026-07-11** in `1e37d72` per Q3=A. Theme + Gallery side by side; Developer Mode, Clear Data, Version and Check for Updates fold into a collapsed `⚙️ App` `<details>`; tysiacha drawer scroll height dropped 1523px → ~800px. Verified in tysiacha, durak, and the root gallery. |
| p1-21 | Durak: transfer-or-beat choice popup | S | ✅ | **Shipped 2026-07-11.** From Yev's playtest (vault durak inbox): a trump card matching the attack rank can either transfer or beat, but the tap handler auto-transferred — the choice was taken away. Now `#choice-overlay` (card face + amber "⇄ Transfer" / blue "🛡 Beat" buttons, tap-outside cancels, hidden on drawer open/restart) appears only when BOTH moves are legal for the tapped card; single-option cards play immediately as before. Verified both paths + cancel in browser. |
| p1-20 | Quick game switcher in the drawer | S | ✅ | **Shipped 2026-07-11** in `1e37d72`, from questionnaire Q4=A (vault inbox idea). "Games" row of 10 emoji icons (44px targets, horizontal scroll) above the token row; current game highlighted; gallery-relative hrefs work from root and game pages. Waterfall intentionally excluded (not listed in the gallery either). Now 7 icons since p1-29/p1-30 (2026-07-12). |
| p1-29 | Demote durak-alchemist, durak-dungeon, durak-tactics to the Lab | M | ✅ | **Shipped 2026-07-12** in `e56cad2` (Settings & Automation Cleanup Sprint, Phase 1). Cards, GAMES_META/SCORES_META rows, 3D portals (`shared/3d/constants.js`), and drawer-switcher entries removed; `drafts/index.html` gained a "Shelved from the arcade" section linking all three at their unchanged `games/` URLs. CLAUDE.md + games/CLAUDE.md tables annotated Lab, GEMINI.md regenerated. Root gallery = 7 invest games, switcher matches; 169 tests + 13-page smoke green; gallery/Lab/drawer verified in browser. Open follow-up: Lab games' drawer-section convention = Q7 of the settings questionnaire. |
| p1-30 | Remove waterfall from the arcade | S | ✅ | **Shipped 2026-07-12** in `e56cad2`. `games/waterfall/` deleted (code stays in git history), `lastPlayed_waterfall` dropped from `clearAllGameData`, smoke comment + CLAUDE.md/games/CLAUDE.md rows removed, GEMINI.md regenerated. `grep -ri waterfall` now hits only this roadmap and `docs/archive/new-game-thoughts.md` (historical record, intentionally untouched). |

### P2 — Medium Features

| ID | Item | Effort | Status | Notes |
|----|------|--------|--------|-------|
| p2-08 | Durak: match history and stats | S | ✅ | Shipped 2026-07-07 in `36f6085`. Seat 0 (device-owner human) win/loss/draw record tracked in `durak_wins`/`durak_losses`/`durak_draws`, shown on the game-over overlay. |
| p2-09 | Waterfall: wave structure | M | ❌ | Closed 2026-07-12 — waterfall's taste-questionnaire verdict is **delete** (p1-30); no further work. |
| p2-11 | Durak: perevodnoy (transfer) variant | M | ✅ | Shipped 2026-07-10. Rules toggle at game setup: defender may transfer the attack to the next player by adding a card of the same rank; AI must play the variant correctly. Includes first turn transfer toggle. Enforces constraint that defender must have enough cards to take the full transfer. |
| p2-14 | Durak: Coach Hints + Game Log | M | ✅ | Shipped 2026-07-10. Built Coach Hints using Hard AI logic dynamically displayed in `#coach-banner` and recorded into `state.log`. Also built Tysiacha-style `logbook` drawer overlay for the full event stream. Supersedes the "referee mode" requirement per latest direction. |
| p2-18 | Tysiacha: AI difficulty setting (was: AI Personalities) | M | ✅ | Shipped 2026-07-10 (commit `0794526`, sprint slice 2/3). Per questionnaire Q6 the setting is the core: Easy/Normal/Hard select in the game's ⚙️ menu, applies live to the next AI decision, persists as `tysiacha_difficulty`. `estimateHand(hand, difficulty)` (exported, unit-tested) drives bidding — easy undervalues marriages and swings wide (timid auctions, occasional wild overbids); hard prices tight and bids to the edge. Play: easy blunders (35% random lowest-card dumps, 25% forgotten marriages, never re-raises); hard protects K+Q pairs when dumping, contests points at a lower threshold, and wins a trick specifically to unlock a pending marriage declaration. The named-personality flavor (cautious Vera vs aggressive Boris) was deliberately cut — captured in the vault Tysiacha Improvements inbox. 3 new tests. |
| p2-21 | Tysiacha: custom player names | S | ✅ | Regressed same day by the Drawer migration (`641732d`) — bindings never attached, nothing saved; **restored 2026-07-11 by p0-12** and, per questionnaire Q1=A, the name inputs moved to the New Match setup screen in p1-17 (still persisted as `tysiacha_name_*`; applied at next match rather than live). Original ship notes: Added and shipped 2026-07-10 on Yev's request (commit `0794526`, with p2-18). Three text inputs in the ⚙️ settings (Player Names section) rename any seat, live, no restart; persist as `tysiacha_name_[seat]` (durak's `durak_name_*` was the prior art). Names flow through `playerName(p)` in i18n.js, so defaults stay localized (You/Vera/Boris ↔ Вы/Вера/Борис) and a custom name wins in both languages; the exchange lines use case-neutral «игроку <имя>» phrasing so Russian never needs to decline a custom name. Renaming seat 0 flips log grammar to third person ("Yev passes" / "Yev пасует"). |
| p2-20 | Tysiacha: trick audit rows + deal recap | S | ✅ | Shipped 2026-07-09. Sprint slice 3/3 — completes the game-log design. In the 📜 drawer, each completed trick renders as an audit row: T-number chip + the 3 cards as mini card chips in play order (leader tagged "led", winner gold-bordered, player name under each) + winner/points/reason column, replacing the plain play/trick-won lines (in-progress tricks still show as lines). Deal-end summary gains a "📜 Review deal" button; #logbook gets z-index 60 so it stacks above the summary and closing returns to it. Rendering-only change (no gameplay/log edits) — 149 tests green, verified in browser: audit row correct (A♣ led/won marked), review button opens drawer over summary, zero console errors. |
| p2-22 | Durak: Auto Play Visualizer | M | ✅ | **Shipped 2026-07-11**. Implemented passive auto-play mode ("Spectate AI Match") with Auto-Restart toggle. Modifies `scheduleAiAction` to play for the human seat when enabled, and adds toggles to the start screen Rules and the settings drawer. Human takeover works seamlessly because tapping a card manually fires the action and `ai.js`'s tick loop doesn't block it. See vault note `Kameko Studio — Auto Play Visualizers.md` |
| p2-23 | Tysiacha: Auto Play Visualizer | M | ✅ | **Shipped 2026-07-11**. Implemented "Simulated Tournament" auto-play mode (3 AIs). Supported Auto-Restart, Fast Forward speed, and human takeover. Settings live in the drawer. |
| p2-24 | Keypad Quest: Auto Play Visualizer | M | ✅ | **Shipped 2026-07-11**. Implemented "Virtual Typist" auto-play that types answers directly into the active input mode buffer. Added Spectate (Auto-Play) button to start menu and Auto-Play / Auto-Restart toggles in settings drawer. See vault note `Kameko Studio — Auto Play Visualizers.md` |
| p2-25 | Materials Run: Auto Play Visualizer | M | ✅ | Shipped 2026-07-11 in 72cd18a. Implemented pathfinder bot that navigates safely. | Implement a greedy pathfinding bot (for Score/Survival) that navigates the grid. Support Auto-Restart and manual takeover. See vault note `Kameko Studio — Auto Play Visualizers.md` |
| p2-26 | Blob Zapper: Auto Play Visualizer | M | ✅ | **Shipped 2026-07-11** in 4cc8c44. Implement automated defense grid that zaps dangerous blobs procedurally. Support Auto-Restart and manual takeover. See vault note `Kameko Studio — Auto Play Visualizers.md` |
| p2-27 | Durak: spectate upgrades — reveal hands + speed setting | M | ✅ | **Shipped 2026-07-12** in `b5ce1af`. New "Automation" drawer section (Auto Play moved here from Quick Actions): Slow/Normal/Fast speed select scales the AI thinking delay (`speedMultiplier` in `ai.js`, unit-tested), and a "Reveal All Hands" toggle renders every opponent's actual hand face-up via `createCardEl` instead of a card-back + count. Per Q4 of the open-decisions questionnaire, reveal only takes effect while Auto Play is on (separate toggle, default off) — taking over hides hands again on the next render. Verified live: reveal shows/hides correctly on toggle and on takeover, speed produces a ~4.7x difference in observed AI-move rate over a 5s sample (fast vs slow), both settings persist across reload, zero console errors. |

### P3 — New Games

| ID | Item | Effort | Status | Notes |
|----|------|--------|--------|-------|
| p3-04 | Tysiacha (1000) — draft in the Lab | M | ✅ | Promoted to `games/tysiacha/` on 2026-07-09. All phases (split, tests, token/settings hooks, PWA metas, root registrations) complete. 130 tests covering `constants.js`, `state.js`, and `gameplay.js` passing. |

### Backlog — Tech Debt

| ID | Item | Effort | Status | Notes |
|----|------|--------|--------|-------|
| b-16 | Shared drawer: scroll affordance + reset scroll on open | S | ✅ | **Shipped 2026-07-12.** From the vault tysiacha inbox: "hard to tell that side menu is scrollable and does not reset — okay but confusing." Applies to the shared ☰ drawer (`shared/settings.js`/`settings.css`) in every game. Add a visual cue that content continues below (bottom fade/gradient that disappears at scroll end is the standard pattern) and reset `scrollTop` to 0 each open — since b-14, sections re-render per open anyway, so a preserved scroll position no longer buys anything. *Done when:* an overflowing drawer visibly signals more content below, opens scrolled to the top every time; verified in tysiacha (longest drawer) and the root gallery. |
| b-02 | ES module split: materials-run | L | ✅ | Shipped 2026-07-08. Split into `constants.js`/`state.js`/`gameplay.js`/`main.js` (no `ui.js` — rendering is tightly coupled to physics/state updates throughout, so merged into `gameplay.js` following the `durak-tactics` pattern). Also extracted the inline `<style>` block into `style.css` to match the documented per-game file convention (index.html was 712 lines with all CSS+JS inline; now 72 lines of markup). Verified in browser: Score Attack and Survival modes both playable, pin placement/physics/shooting/collision/danger-zone/game-over/top-scores all working, zero console errors. |
| b-03 | ES module split: hidden-object | L | ✅ | Shipped 2026-07-08. Split into `constants.js`/`state.js`/`gameplay.js`/`main.js` (no `ui.js` — canvas rendering tightly coupled to physics/collision state, merged into `gameplay.js` like `durak-tactics`). Extracted the inline `<style>` block into `style.css` too. index.html was 782 lines with all CSS+JS inline; now 72 lines of markup. Verified in browser: round timer, object physics/collisions/blinking, target detection, success/game-over modals, and settings pause/resume all work with zero console errors. |
| b-04 | ES module split: blob-zapper | M | ✅ | Shipped 2026-07-08. Split into `constants.js`/`state.js`/`gameplay.js`/`main.js` (no `ui.js` — canvas rendering tightly coupled to physics/particle state throughout, merged into `gameplay.js` like `durak-tactics`) plus extracted `style.css`. Done after b-08 (pointer-event consolidation) so the module split didn't have to redo that work. Verified in browser: blob physics/collisions/zapping, combo scoring, push button, drag interaction, and game-over/new-best UI all work with zero console errors. |
| b-05 | ES module split: waterfall | M | ❌ | Closed 2026-07-12 — waterfall's taste-questionnaire verdict is **delete** (p1-30); no point splitting a game being removed. |
| b-06 | CLAUDE.md / GEMINI.md sync discipline | S | ✅ | Shipped 2026-07-08. Director chose the "single source of truth" option over a diff-check-only hook. Every `GEMINI.md` (root, `docs/`, `drafts/`, `games/`) is now **generated** from its `CLAUDE.md` counterpart by `node scripts/generate-context-docs.js` — never hand-edit a `GEMINI.md`. Tool-specific content stays inline in `CLAUDE.md`, wrapped in `<!-- GEMINI-OVERRIDE:key -->...<!-- /GEMINI-OVERRIDE -->` markers; replacement text lives in `context-src/gemini-overrides.md`, keyed `pair:key`. `--check` mode verifies all four are in sync (non-zero exit if stale) — a natural pre-commit hook candidate later, not wired up automatically per the "never touch git config" rule. Also reconciled 3 pre-existing wording drifts found in `games/CLAUDE.md` vs `games/GEMINI.md` (not tool-specific, just staleness) directly into the canonical `games/CLAUDE.md` text. |
| b-07 | Durak: test coverage for ai.js | M | ✅ | Already done — `tests/durak.test.mjs` has 3 dedicated AI-logic tests via the exported `_test_aiTurn` (Normal-difficulty defense, Hard-difficulty take-vs-defend endgame branches). One of the three currently fails, but that's a bug (tracked as p1-07), not a coverage gap. Found stale during the 2026-07-07 roadmap audit. |
| b-08 | Consolidate to pointer events: blob-zapper, hidden-object, waterfall | M | ✅ | blob-zapper fixed 2026-07-08 — the real double-fire bug. Replaced 7 split mousedown/mousemove/mouseup + touchstart/touchmove/touchend/touchcancel listeners (tracking `'mouse'` vs per-touch identifiers in one `touchPoints` array) with 4 unified pointerdown/pointermove/pointerup/pointercancel listeners keyed on `e.pointerId`; added `canvas.setPointerCapture()` on pointerdown so drags tracked correctly even off-canvas (replacing the old window-level `mouseup` workaround) and `touch-action: none` on the canvas. Verified in browser: drag interaction still pushes blobs, zero console errors. hidden-object and waterfall intentionally left alone — confirmed during the 2026-07-07 /improve scan to not have the double-fire bug (hidden-object already debounces click+touchstart; waterfall is touchstart-only), so converting them was a style-only change out of scope for this fix. |
| b-09 | Add `100dvh` fallback pattern to games missing it | S | ✅ | Fixed 2026-07-08. Added a `dvh` declaration immediately after each `100vh` full-screen container rule (vh first as fallback, dvh second to override in supporting browsers) in `blob-zapper`, `hidden-object`, `materials-run`, `waterfall`, root `index.html`, and `3d.html`. Left non-100vh viewport units alone (e.g. 3d.html's `max-height:85vh` overlay, waterfall's `95vh`/`70vh` sub-containers) as out of this item's stated scope. Verified hidden-object renders correctly in browser after the change. |
| b-10 | durak-dungeon: relic-slot tap target under 44px | S | ✅ | Fixed 2026-07-08. `.relic-slot` bumped from 32×32px (28px/36px at responsive breakpoints) to a uniform 44×44px at all breakpoints; `#relic-bar` min-height bumped to match. Safe to do without overflow concerns — `state.run.relics` caps at 5 equipped relics per run (confirmed via `main.js`'s "Relics: X/5" display), not the 15 total relic *types* in the pool as the original note implied. Verified visually in browser: 5-slot relic bar renders with no wrapping or overflow. |
| b-11 | Delete stale `patch_style.py` from repo root | S | ✅ | Deleted 2026-07-08. Confirmed zero references anywhere in the repo before removal (grep for `patch_style` matched only this roadmap row). |
| b-12 | durak-alchemist: settings gear/overlay never renders | S | ✅ | Fixed 2026-07-08. Moved `shared/settings.js` from `<head>` to right before `</body>` (after `main.js`), matching every other game's convention. Verified in browser: gear icon renders top-right, settings overlay opens with tokens/theme/gallery/dev-mode sections, no console errors. |
| b-14 | Drawer API: game-section lifecycle (re-render on open) | S | ✅ | **Shipped 2026-07-11** in `cdedb6e`. registerSection now keeps a registry and rebuilds every section on each drawer open; `title` may be a function, optional `when()` hides a section contextually; all four games (tysiacha, durak, keypad-quest, durak-dungeon) dropped their remove-on-close hacks and register once at boot. Original spec: `KamekoSettings.registerSection()` renders a section exactly once (early-returns if the id exists), so control values go stale on reopen; tysiacha works around it by deleting its sections on `settingsClosed` and re-registering on `settingsOpened` — a hack no other game copies. Give the API a real lifecycle: re-run `render` (or a new `onOpen(container)` callback) every time the drawer opens, then delete tysiacha's remove-on-close workaround. *Done when:* a value changed in-game (e.g. coach toggled via gameplay) shows correctly on next drawer open in tysiacha AND durak without any game-side section removal; all games verified opening twice. |
| b-15 | `clearAllGameData`: cover post-migration localStorage keys | S | ✅ | **Shipped 2026-07-11** in `4fcdd5d`. Full exact-key list plus prefix clearing for dynamic-suffix keys (`tysiacha_name_*`, `durak_name_ai_*`, `durak_name_hotseat_*`, `keypadQuestBestTime_*`); verified localStorage is empty after Clear All Game Data. Original spec: The `keysToRemove` list in `shared/settings.js` predates recent ships and misses: all `tysiacha_*` keys (`tysiacha_name_0..2`, `tysiacha_lang`, `tysiacha_difficulty`, `tysiacha_muted`, `tysiachaHighScore`, `lastPlayed_tysiacha`), durak's stats/settings (`durak_wins/losses/draws`, `durak_name_*`, coach/perevodnoy keys), `blobZapperHighScore`, and other keys added since. Either enumerate them or switch to prefix-based clearing with an explicit allowlist of what survives. *Done when:* after Clear All Game Data, `localStorage` contains no game keys (verified in console across 3 games). |
| b-25 | Watch Mode Takeover bugs: Tysiacha and River Run | S | ✅ | Shipped 2026-07-12 in b362a8c. Tysiacha AI continued to play even after watch mode was turned off because later() resumed the scheduled AI move. River Run was missing the unified Watch Mode drawer integration. Both fixed and pushed. |
