# Kameko Studio — Roadmap

Engineering director: Yevster  
Last updated: 2026-09-22

AI agents read this file to pick the next work item. It holds open work only: rows marked 🚧 are in progress, all others are open. Shipped and closed rows, and the dated session log, live in [`archive/roadmap-history.md`](archive/roadmap-history.md).

Priorities: P0 (foundation/blockers) → P1 (high-impact polish) → P2 (medium features) → P3 (new games) → Backlog (tech debt).

> **Direction — tiers are set (2026-07-12).** The verdict-driven loop ([Kameko Studio — Agent Game Loop Roadmap](planning/agent-game-loop.md)) now has its tiers, from the answered [Kameko Studio Questionnaire — Taste and Tiers](questionnaires/taste-and-tiers.md):
> **Invest:** durak (the benchmark), tysiacha (keep verdict), keypad-quest, materials-run, river-run, blob-zapper (controls need rework — p1-31), hidden-object. **To the Lab:** durak-alchemist, durak-dungeon, durak-tactics (p1-29 — "mixed mechanics interesting, not worth the main-page spot"). **Delete:** waterfall (p1-30).
> Jam steering: prototype bar = **Modest** (playable + phone-comfortable + restart), cadence = **on-demand only**, what makes durak good = nostalgia/cultural connection + 5–10 min sessions. Jam genre directions (Q3) are still unanswered — the brief (p0-08) should note that. Per-session playtest verdicts land in the [Kameko Playtest Log](playtest-log.md) — read it before triaging. Sprint history lives in the roadmap history and, in full detail, in the frozen dev logs (`archive/dev-logs/`).

---

## P0 — Foundation

Pre-conditions that should be cleared before adding features.

| ID | Item | Effort | Status | Notes |
|----|------|--------|--------|-------|
| p0-16 | Obsidian Intranet: docs organization, interlinking & Studio Dashboard | M | open | **Next up — prioritized immediately following closeout of current iteration** (Yevster steer, 2026-09-22). Turn `docs/` into an interconnected Obsidian digital garden/intranet. 1) Upgrade `docs/README.md` into a Studio Dashboard / Map of Content (active games gallery grouped by tier, open questionnaires, roadmap tiers, steering links). 2) Standardize YAML frontmatter/properties (`title`, `tier`, `status`, `tags`) on game docs and questionnaires. 3) Cross-link roadmap items, playtest log entries, questionnaires, and game specs with standard relative Markdown links (GitHub- and agent-safe). *Done when:* `docs/README.md` acts as an Obsidian hub; all game READMEs have valid YAML frontmatter and tags; cross-links between roadmap, games, questionnaires, and playtest logs resolve cleanly on both GitHub and Obsidian; `npm test` remains green. Spec: `docs/planning/ideas.md`. |

---

## P1 — High-Impact Polish

Biggest visible improvements to the player experience.

| ID | Item | Effort | Status | Notes |
|----|------|--------|--------|-------|
| p1-37 | Arcade chrome: "Back to home" reachable without scrolling | S | open | From the Improvements inbox (2026-07-14): getting back to the arcade home currently requires scrolling. Agent first verifies where the back-to-home link sits in each gallery game today, then moves or duplicates it into always-visible chrome (top header or the settings drawer) consistently across games. *Done when:* on a phone-size viewport, every gallery game offers a way back to the arcade home without any scrolling; verified in browser. |
| p1-05 | River Run: ghost run | M | open | Record best run's boat positions in localStorage, replay as transparent ghost on next attempt |
| p1-06 | Hidden Object: hint system (free) | S | open | Hint button highlights the quadrant containing the target. **Rescoped 2026-07-12 (Q4=E, tokens removed):** no token cost — free, limited to ~2 hints per round so it stays a game and not a walkthrough (limit is the agent's judgment call, note it when shipping). Do after Sprint 2. |
| p1-22 | Durak: tap-to-target defense | M | open | From the questionnaire [Durak Questionnaire — Perevodnoy UX](archive/questionnaires/durak-perevodnoy-ux.md) **Q1=B** (answered; picked up 2026-07-12). After a transfer, 2–3 attacks are open and each tapped defense auto-covers the first open attack it can beat; Yev wants explicit pairing: tap a card, then tap the attack it should cover. Keep the fast path — when only one attack is open (the normal non-transfer case) a tap should still play instantly. *Done when:* with 2+ open attacks, tapping a legal defense highlights the attacks it can cover and the card lands on the tapped one; single-open-attack behavior unchanged; pairing covered by unit tests. |
| p1-23 | Durak: inline transfer/beat choice (replace modal) | S | open | From the questionnaire [Durak Questionnaire — Perevodnoy UX](archive/questionnaires/durak-perevodnoy-ux.md) **Q2=C** (answered; picked up 2026-07-12). The ⇄ Transfer / 🛡 Beat popup (p1-21) is a centered modal covering the table; Yev wants small inline Transfer/Beat buttons above the hand instead. Same trigger rule (only when a card is legal as both); tapping elsewhere cancels. Coordinate with p1-22 — both touch the defense tap handler. *Done when:* dual-purpose card tap shows the inline buttons (no modal), single-purpose cards play instantly, cancel works; verified in browser. |
| p1-25 | Durak: coach lines in the 📜 log — distinct color + hide toggle | S | open | From the durak [idea inbox](games/durak/ideas.md). Coach hints are logged as `coach_hint` events whenever coach is on (`ui.js` L405) and render like ordinary lines. Style them distinctly (tysiacha's italic-teal hint styling is the in-repo prior art) and add a "show hints" filter toggle inside the logbook drawer so the log can be read as pure game events. *Done when:* coach lines are visually distinct, the toggle hides/shows them live and persists, verified in browser. |
| p1-26 | Tysiacha: turn/phase + contract HUD | M | open | From the tysiacha [idea inbox](games/tysiacha/ideas.md) (two items merged: "indicator that it's your turn and what I need to do" + "show the bet you need to beat"). Verified: the only cues today are the transient banner and per-seat `bidLabel` chips that render only during bidding/talon phases (`ui.js` L261); nothing persistent says whose turn it is, what action is expected, or what number is live. Add a persistent status chip: phase + expected action ("Your bid", "Give 2 cards", "Your lead"), current high bid + bidder during the auction, declarer's contract during play. Full en/ru via i18n.js. *Done when:* at any point mid-deal a screenshot answers "whose turn, doing what, against what number"; verified in both languages. |
| p1-27 | Tysiacha: more sounds + phone-speaker pass | S | open | Confirmed by the questionnaire [Tysiacha Questionnaire — Language, Difficulty & Sound](archive/questionnaires/tysiacha-language-difficulty-and-sound.md) **Q6** (answered; picked up 2026-07-12): trick-sweep whoosh, match-win fanfare, your-turn nudge — each ~5 lines in `sfx.js`. Fold in the inbox note to tune levels on a real phone speaker (current levels tuned on desktop). *Done when:* three new sounds fire at the right moments, respect the mute toggle, and levels are sanity-checked on mobile. |
| p1-28 | Tysiacha: match-end "New match" goes through the setup screen, free | S | open | Questionnaire **Q9=A** (2026-07-12): the match-end "New match" button (`sum-next` in `main.js`) still charges 1 🪙 while the New Match setup screen restart is free — inconsistent and dodgeable. Route the match-end button to the setup screen like every other restart; no token charge. *Done when:* finishing a match and starting a new one costs 0 tokens and lands on the setup screen; verified in browser. |
| p1-36 | Browser e2e tests: core use cases for all gallery games | M | open | From the Improvements inbox ("Ui end to end tests to test core use cases of games", 2026-07-12). The harness now exists: `npm run e2e` (`scripts/e2e.mjs`, puppeteer-core — shipped with the watch-mode fixes, 10 tests covering Watch Mode start/stop/take-over, Clear All Game Data, Lab free-play). Extend it to one core-flow test per gallery game: start a game, perform the basic interaction (play a card / answer a prompt / move the boat / tap a cell), reach or simulate game over, reopen the settings drawer. Keep tests behavior-level (no pixel assertions) so they survive visual polish. State probes: dynamic-`import()` the game's `state.js` for module games; top-level `let` bindings for inline-script games (documented in CLAUDE.md Testing). *Done when:* every gallery game has at least one passing core-flow e2e test and the suite is listed alongside smoke in the definition of done for ships touching game flows. |
| p1-31 | Blob Zapper: rework the control scheme | M | open | **Game moved to the Lab 2026-07-15 (p1-41, Yev's call) — still worth doing, but Lab-tier priority now; a control rework is the natural promotion path back.** Taste questionnaire (2026-07-12), Q1 note: "should be something other than push to move. the graphics are cool but the game needs work." Invest verdict with an explicit gripe about the push interaction. Agent proposes a better control (e.g. direct drag/flick of blobs, gravity-well hold, draw-a-wall) — prototype 1–2 in-game behind a setting if cheap, pick the best feel, make it the default. Yev playtests and verdicts. *Done when:* a new default control ships that doesn't rely on push-to-move, old behavior removed or demoted to a setting, verified on a phone-size viewport. |
| p1-42 | Materials Run: hit-impact feedback (target-hit burst + enemy-collision flash) | S | open | From the 2026-07-22 modernization planning pass. No visual payoff exists today for either event: `gameplay.js`'s bullet-collision block (~L676-682) just increments score and calls `selectNewTarget()`; `triggerGameOver()` (~L376) is silent apart from the overlay appearing. Add a lightweight DOM particle burst on target hit (spawn 6-10 small divs, animate outward + fade via CSS transition, self-remove — same disposable-element pattern already used for `.pin`/`.bullet`) and a brief impact flash on enemy collision before the game-over overlay shows. Zero new dependencies. *Done when:* both events show a visible, self-cleaning effect with zero leaked DOM nodes after repeated play (20+ hits), verified in browser with no console errors. |
| p1-43 | Materials Run: speed trail behind the player | S | open | From the 2026-07-22 modernization planning pass. The player renders as a single translated div with no motion history — at `MAX_SPEED` (200) on a DOM grid, movement can read as teleport-y rather than skating. Periodically (e.g. every ~40ms while `currentSpeed` exceeds a threshold) drop a fading ghost div at the player's previous position, styled from `--player-color` at low opacity, auto-removing after ~300ms — same disposable-element + CSS-fade pattern as `.pin`. *Done when:* moving fast visibly leaves a short fading trail, trail density scales with speed (denser near `MAX_SPEED`, absent near-stopped), verified in browser with no DOM node leak over a full run. |
| p1-44 | Materials Run: material-crossing transition FX | M | open | From the 2026-07-22 modernization planning pass. `getMaterialAtPixel()` (`gameplay.js`) already computes the player's current material every frame but nothing happens when it changes. Track `state.lastMaterial` and fire a one-shot cue at a material-boundary crossing — e.g. a splash ring entering water, a sparkle/shimmer entering ice, a dust puff entering sand (grass = neutral, no effect). Pure visual, no physics change — p2-06 reuses this same crossing-detection hook for the actual momentum/skid mechanics later. *Done when:* crossing each material boundary shows a distinct, material-appropriate one-shot effect; re-entering the same material or crossing into grass doesn't spam it; verified in browser. |
| p1-45 | Materials Run: textured material tiles (CSS-only) | S | open | From the 2026-07-22 modernization planning pass. `.cell.grass/.water/.ice/.sand` (`style.css`) are flat `background-color` blocks — functional but visually dated next to the rest of the arcade. Replace with CSS-only gradients/patterns per material (durak's p2-32 card-back/table-theme work is the in-repo zero-asset precedent — no image assets) so each material reads distinctly beyond color alone. Keep dark/light mode variants working (colors are already CSS custom properties). *Done when:* all four materials are visually distinguishable via texture/gradient, both theme modes look correct, no measurable frame-rate regression on the 20×20 (400-cell) grid. |
| p1-46 | Materials Run: danger-zone tension escalation | S | open | From the 2026-07-22 modernization planning pass. The only danger-zone cue today is `.danger-zone-blinking` — a flat 0.5s outline blink for the entire `DANGER_ZONE_DURATION` (7s), with nothing communicating the `DANGER_ZONE_GRACE_PERIOD` (500ms) countdown once the player is actually inside it before `triggerGameOver()` fires. Escalate the cue as `state.timePlayerInDangerZone` climbs toward the grace-period limit inside `update()` (faster blink and/or a screen-edge vignette), giving a clear "about to die" signal instead of same-intensity blink until sudden game over. *Done when:* being inside the zone visibly escalates over the 500ms grace window before game over, verified in browser. |
| p1-47 | Materials Run: verify and tighten full-screen use on phone | S | open | Improvements inbox item ("more full screen on phone"), unverified against current behavior. Agent first screenshots the live game at a real phone viewport (375×812 and similar) to identify the actual gap — candidates: the grid's `max-width: 95vw` plus body padding leaving unused edge space, the floating score/timer/button chrome eating vertical room above `#game-container`'s `margin-top: 60px`, or the Fullscreen API never being requested anywhere (so the mobile browser chrome/address bar never hides). Fix whichever is the real gap — CSS tightening, a Fullscreen API toggle, or both. *Done when:* on a phone-size viewport the grid visibly uses more of the available screen than it does today, verified with before/after screenshots. |
| p1-48 | River Run: game-over best-score display | S | open | From the 2026-07-22 modernization planning pass. Game-over overlay shows only `Final Score: N` — `riverRunHighScore` is written to localStorage every game over (`gameOver()`) but never rendered or compared. Add a best-score line to the overlay and a "New Best!" treatment when the run's score beats the stored high score, matching the gold pulse/badge pattern p1-08 used for the other Invest games. *Done when:* the game-over overlay always shows both the run's score and the stored best; beating the best shows a distinct "New Best!" treatment; verified in browser across a beaten and a non-beaten run. |
| p1-49 | River Run: collision impact feedback | S | open | From the 2026-07-22 modernization planning pass. Hitting an obstacle ends the run with a silent `Tone.Transport.stop()` and no visual punch — `triggerExplosion` never fires on the boat's own death, only on shot obstacles. Add a brief camera shake + red screen flash + a synthesized crash SFX (Tone.js, zero assets, same convention as tysiacha's `sfx.js`) on the fatal collision, and reuse `triggerExplosion` at the boat's position. *Done when:* a fatal collision visibly shakes the camera and flashes the screen, plays a distinct crash sound when unmuted, and the existing explosion particles fire at the boat; verified in browser with sound on and off. |
| p1-50 | River Run: destroy SFX for shot obstacles | S | open | From the 2026-07-22 modernization planning pass. Shooting an obstacle triggers `triggerExplosion` (particles) but plays no sound at all. Add a short synthesized impact SFX via Tone.js (zero assets) on every successful hit in `updateGame()`'s projectile-collision branch, respecting the existing mute toggle. *Done when:* every obstacle destroyed by a projectile plays a distinct sound when unmuted, is silent when muted, and doesn't stack/distort when multiple obstacles are hit in quick succession; verified in browser. |
| p1-51 | River Run: near-miss cue | S | open | From the 2026-07-22 modernization planning pass. The boat can pass within a sliver of an obstacle with zero feedback beyond survival. Add a "whoosh" SFX plus a brief edge-of-screen flash when an obstacle passes the boat within a small margin (e.g. half the current collision threshold) without hitting it. Visual/audio only — no score change (a scored version is p2-43, gated on the questionnaire). *Done when:* a deliberately tight dodge (verified via a temporary debug log of the closest pass distance) reliably triggers the cue and a comfortable-margin pass does not; score is unaffected either way; verified in browser. |
| p1-52 | Keypad Quest: in-game help/rules overlay | S | open | From the 2026-07-22 sprint planning pass. No in-game rules explanation exists beyond the menu subtitle ("Build your defense by answering questions") — unlike Maze Warden's ❓ or Astro Salon's rules card, nothing explains that towers unlock via answer streak, that Hint resets your streak, what Chill/Play/Watch modes do, or how to switch/manage decks. No creative fork — add a ❓ button (topbar, matching the shared arcade chrome pattern) opening a dismissible overlay covering those points. *Done when:* the overlay is reachable mid-run without losing state, covers streak→tower unlock, Hint's streak-reset cost, the three play modes, and deck switching; verified in browser. |

---

## P2 — Medium Features

New mechanics that add replayability.

| ID | Item | Effort | Status | Notes |
|----|------|--------|--------|-------|
| p2-01 | Arcade Passport (achievement system) | L | open | Cross-game achievements stored in localStorage, passport-stamp UI on index.html dashboard |
| p2-02 | Daily Arcade Challenge | M | open | Date-seeded featured challenge per game on 7-day rotation, shown on dashboard. **Reward framing updated 2026-07-12 (Q4=E):** no token bonuses — reward is streaks/badges (pairs with p2-01's achievement stamps). |
| p2-03 | Keypad Quest: visual wave map | M | open | 3-second interstitial between waves showing upcoming enemy types/counts. **Rescoped 2026-07-22:** only one enemy archetype exists today, so "upcoming enemy types" has nothing to show yet — real value here waits on p2-47 (enemy variety). Until then, downscope to a brief pre-wave stat readout (enemy count, HP multiplier vs. wave 1, speed multiplier) — still shippable now, no creative fork, just less impactful standalone than paired with p2-47. |
| p2-04 | Keypad Quest: tower upgrades | M | open | Spend in-game currency (kills) on fire rate / splash / range upgrades. Note: `upgradeRandom()` in gameplay.js already applies an automatic random tower level-up every 10 correct answers — that's not player-chosen or currency-spent, so the actual gap (player agency over which upgrade) is still unbuilt. Confirmed during 2026-07-07 roadmap audit. **Enriched 2026-07-22:** three concrete shapes now on the table (build as originally scoped and drop the automatic level-up / keep the automatic level-up and add a separate optional currency layer / leave it alone) — gated on Q3 of the new [Keypad Quest Questionnaire — Sprint Direction](questionnaires/keypad-quest-sprint-direction.md). |
| p2-45 | Keypad Quest: player-chosen tower type | M | open | From the 2026-07-22 sprint planning pass. `typeForStreak()` alone decides which of the 4 tower types (Basic/Rapid/Sniper/Splash) gets built on a correct answer — the player never chooses; the tower-strip UI already shows live progress toward the next streak-gated unlock. Whether to let the player pick among already-unlocked types, decouple type from streak entirely, or leave it alone is a real creative fork. Gated on Q2 of the new [Keypad Quest Questionnaire — Sprint Direction](questionnaires/keypad-quest-sprint-direction.md). |
| p2-46 | Keypad Quest: a real fail/pressure state | M | open | From the 2026-07-22 sprint planning pass. Enemy HP (×1.2/wave) and speed (×1.03/wave) scale forever, but nothing bad happens if enemies aren't killed in time — they just keep orbiting; there is no lose condition today (confirmed a side effect of a 2026-07-11 fix removing a dead Auto-Restart switch, not a deliberate zero-stakes design call). Whether to add real stakes, keep it zero-stakes on purpose and lean on feel/pacing instead, or split by mode (Chill stays zero-stakes, Play gets real stakes) is a real creative fork. Gated on Q1 of the new [Keypad Quest Questionnaire — Sprint Direction](questionnaires/keypad-quest-sprint-direction.md). |
| p2-47 | Keypad Quest: second enemy type/behavior | M | open | From the 2026-07-22 sprint planning pass. Only one enemy archetype exists (`makeEnemy()`) — a plain dot, HP/speed scale with wave, no behavior variety at all, unlike Maze Warden's Bomber or Materials Run's planned hunter enemy. Whether to add a second enemy (and whether it should be tied to a new fail state from p2-46, or work standalone as a pacing beat) is a real creative fork. Gated on Q4 of the new [Keypad Quest Questionnaire — Sprint Direction](questionnaires/keypad-quest-sprint-direction.md). |
| p2-48 | Keypad Quest: sound layer | S | open | From the 2026-07-22 sprint planning pass. Zero audio anywhere in the game today (confirmed via code search — no `AudioContext`/`audio`/`sfx` reference in any of the 7 source files) — no correct/wrong cue, no tower-fire/enemy-death sound, no wave-clear fanfare, despite a decent existing particle/ripple/float-text FX layer (`fx.js`) to pair cues with. Whether to add a basic WebAudio layer now, defer entirely, or add just 1-2 key cues is a scope/priority call, not an agent judgment call — same fork Materials Run's modernization pass just asked. Gated on Q5 of the new [Keypad Quest Questionnaire — Sprint Direction](questionnaires/keypad-quest-sprint-direction.md). |
| p2-49 | Keypad Quest: built-in deck expansion | S | open | From the 2026-07-22 sprint planning pass. Only 3 built-in decks today (World Capitals 20 pairs, Multiplication 12, Elements 16); custom/imported decks already cover personal use cases, so this is only about what ships by default. Specific topics are a taste call, not an agent judgment call. Gated on Q6 of the new [Keypad Quest Questionnaire — Sprint Direction](questionnaires/keypad-quest-sprint-direction.md). |
| p2-05 | Hidden Object: themed scenes | L | open | Layered scenes (kitchen, forest, space) instead of flat emoji grid |
| p2-06 | Materials Run: material combos | M | open | Cross-material transitions add momentum-affecting mechanics on a boundary crossing, not just visual (p1-44 ships the crossing-detection hook + visuals first — reuse `state.lastMaterial`): e.g. ice→sand = a brief skid (velocity partially preserved against sand's own friction), water→ice = a frozen-slide burst (temporary near-zero friction). Exact effect list and intensity — mild flavor vs. leaning into Yev's stated love of "chaotic" play — is gated on the [Materials Run Questionnaire — Modernization Direction](questionnaires/materials-run-modernization.md); do after p1-44 lands. |
| p2-07 | Durak: smarter AI with tells | M | open | Hesitation delays and a "thinking..." indicator already exist (`ai.js` `scheduleAiAction`: 500–900ms random delay; `ui.js` `status-thinking` class). Remaining gap: personality variation per AI seat is still unbuilt. Confirmed during 2026-07-07 roadmap audit. **Confirmed via Q1 of the open-decisions questionnaire (2026-07-12):** "named opponents with distinct styles makes the table feel alive" — build it, difficulty alone isn't enough. One opponent hoards trumps, another attacks recklessly, with hesitation "tells" to read — pairs naturally with tysiacha's p2-31 (same ask, other game). |
| p2-10 | River Run: biome transitions | M | open | Visual storytelling as score increases: forest → canyon → volcanic → space. Baseline scope is visual-only (fog/water-tint/lighting/geometry-color changes, no gameplay change) — whether biomes should also affect gameplay (e.g. volcanic hazards, space drift) is Q6 of the new [River Run Questionnaire — Modernization Direction](questionnaires/river-run-modernization.md); the visual-only version is agent-shippable without waiting on it. |
| p2-12 | Durak: seeded deals + share link | M | open | Optional seed (URL param + "share this deal" button) producing a deterministic shuffle so a deal can be replayed or shared by link. Needs a seeded RNG — durak-dungeon's mulberry32/seedFromString are the in-repo prior art. From the durak [idea inbox](games/durak/ideas.md). **Confirmed via Q3 of the open-decisions questionnaire (2026-07-12):** "I'd use this" — build it soon. |
| p2-13 | Durak: extended stats | S | open | Beyond p2-08's W/L/D: wins as attacker vs defender, take rate, average game length, current streak; small stats view reachable from the game-over overlay or settings. From the durak [idea inbox](games/durak/ideas.md) ("Stats"). **Decision made (questionnaire Q6=A, 2026-07-12): keep classic win semantics** — not-the-Durak = W stays as is; extended stats should *additionally* track placement counts (1st/2nd/…/Durak, from `state.finishOrder`) so the placement story is visible without changing what W means. Unblocked. |
| p2-15 | Tysiacha: Scripted First Deal + expanded tutorial | M | open | A stacked tutorial hand that forces a marriage, a trump scenario, and a bid decision to ease new players into the game mechanics. **Confirmed top teaching pick via Q2 (2026-07-12)**: "I want a tutorial setting that expands on the rules" — pair the scripted hand with a tutorial mode that layers extra in-context explanation on top of the existing rules overlay (e.g. inline call-outs at the marriage/trump/bid moments it forces), not just the stacked deal alone. Ship before p2-16. *Done when:* a new player can start a "tutorial" match that deals the forced hand and explains each of the three moments as they happen, toggled off for normal play. |
| p2-16 | Tysiacha: Post-Deal Analysis | S | open | A one-line hint at the end of a deal explaining the biggest missed opportunity or best play made. Deprioritized behind p2-15 per Q2 (2026-07-12) — pick up after the scripted first deal ships. |
| p2-17 | Tysiacha: Match Stats & History | M | open | Track bids made/failed rate, average points per deal, and win streaks. |
| p2-19 | Tysiacha: Seeded Deals & Sharing | M | open | Play a specific deal via a share link (using mulberry32, similar to Durak Dungeon). |
| p2-28 | Tysiacha: spectate upgrades — reveal hands + speed setting | M | merged → p1-32 | **Merged 2026-07-12:** Q1=A/Q2=B make this exactly the tysiacha slice of Watch Mode (Sprint 3). Original scope (Slow/Normal/Fast select replacing binary Fast Forward, "Reveal all hands" toggle, persisted, en/ru) carries into p1-32's spec unchanged — don't implement separately. |
| p2-29 | River Run: cooler auto-mode visualizer | M | open | From the arcade-wide idea inbox: the existing auto mode (Auto-Shoot/Auto-Avoid, inline in `games/river-run/index.html`) just hugs the screen edge — "make it skate around more, not just go to the side." Reconfirmed by the taste questionnaire (2026-07-12): river-run is Invest, "cool visualiser but needs better auto run and maybe other features." Give the auto boat livelier movement: carve lines across the river, weave between obstacles with margin, vary its lane. Consider doing b-01 (module split) first or alongside — the game is still a 1000-line inline monolith. *Done when:* watching auto mode for 60s shows the boat using the full river width with varied, deliberate-looking movement, not edge-hugging; survival not worse than the current bot. |
| p2-30 | Tysiacha: relaxed-bidding setting | M | open | Questionnaire **Q8=D** (2026-07-12): keep the classic forced-100 opener as the default, add a "relaxed bidding" toggle on the New Match screen where the opener is *not* forced — anyone may pass. Needs an all-pass rule: if Raspasy is enabled in Classic Rules, an all-pass deal plays as raspasy; otherwise redeal. AI bidding must respect the mode (easy bots may pass more). Persist with the other match settings (`tysiacha_settings`), full en/ru labels. **Q1 of the open-decisions questionnaire (2026-07-12) confirms this default as-is** — no remaining ambiguity. *Done when:* relaxed mode allows an opening pass, all-pass resolves per the rule above, classic mode unchanged, covered by unit tests. |
| p2-31 | Tysiacha: AI personalities as flavor (cautious Vera vs aggressive Boris) | M | open | Cut from p2-18 when AI difficulty shipped (Easy/Normal/Hard), parked in the Improvements inbox since. **Confirmed via Q3 of the open-decisions questionnaire (2026-07-12):** "yes, add personalities." On top of the existing difficulty setting, give each named bot seat a distinct bid threshold and play style (e.g. Vera undervalues risk and protects marriages, Boris overbids and dumps aggressively) — durak's p2-07 is the same ask for the other game, same session is a natural pairing. *Done when:* Vera and Boris are observably different in bidding aggressiveness and card-dumping choices across several deals at the same difficulty level, covered by unit tests on the bid/play thresholds. |
| p2-35 | Astro Salon: expert mode / difficulty modes | M | open — Q4 answered, needs design pass | Yev's inbox (2026-07-15): "i want more difficulty modes. i don't know anything about astrology so I want to keep building out more features but i want there to be an expert mode that is less obvious and interesting for more advanced players." **Q4 of the [Astro Salon Questionnaire — Promotion Decisions](questionnaires/astro-salon-promotion-decisions.md) is answered** (write-in, 2026-07-15): something a player who already knows astrology would find interesting — it should test what they know and also teach them. This targets a knowledgeable player (test + teach), not a harder version of the beginner content — none of the original preset options (stakes/memory-fade/harder-content/Expert-Evening) map cleanly onto it. Round-3 Q3 already rejected single-tap answering — read-then-confirm stays in every mode. **Not yet agent-shippable:** the write-in names the target player and goal but not the mechanic — next session should do a short design pass (like the Chart Reading Room design note) on what "test and teach an astrology-literate player" looks like in this game's format, then update this row. *Done when:* the mechanic is designed, the chosen expert mode is playable and clearly labeled, and reaching it doesn't complicate the beginner path. |
| p2-32 | Durak: cosmetics — card backs / table themes (free picker) | M | open | Cosmetics themselves confirmed wanted (durak questionnaire Q2, 2026-07-12); **the token-sink framing died with Q4=E (tokens removed) — rescoped to a free picker.** Card backs and table themes/felt colors, CSS gradients/patterns only (zero-asset convention), a picker in the settings drawer, equipped choice in localStorage (`durak_equipped_back`/`durak_equipped_theme`). No purchase/unlock model. Do after Sprint 2. *Done when:* at least 2 card backs and 2 table themes selectable, applied live, persist across reload; verified in browser. |
| p2-37 | Materials Run: hit-streak / combo multiplier (Score Attack) | M | open | From the 2026-07-22 modernization planning pass. Score Attack scoring is flat +1 per bullet hit (`gameplay.js` bullet-collision block, ~L676-682) — no reward for skillful chaining. Add a hit-streak multiplier: consecutive hits within a short window (e.g. reset if `now - lastHitTime` exceeds ~2-3s) escalate score-per-hit, with the current multiplier shown near the score display and a visible reset cue when the streak breaks. Pairs naturally with p1-42's hit-feedback work (a bigger burst at higher multipliers is a strong follow-on, not required for this item). *Done when:* chaining hits visibly increases score-per-hit up to a reasonable cap, the streak display updates live, breaking the streak is visually clear, covered by a unit test on the multiplier math, `node --test tests/` green, verified in browser. |
| p2-38 | Materials Run: second enemy behavior for Survival | M | open | From the 2026-07-22 modernization planning pass. All enemies (`createEnemies()`/`updateEnemies()`, `gameplay.js`) share one behavior — a random-walk to an adjacent cell every ~1.5s (`ENEMY_MOVE_INTERVAL`). Survival's only real threat variety today is the roaming danger zone. The Maze Warden Bomber precedent (a second enemy type added specifically to break a "too easy/samey" playtest verdict) is worth following. Agent's default absent other direction: a slow "hunter" that biases its random-walk toward the player's current cell instead of choosing uniformly at random, visually distinct from the base enemy (`.enemy` color/shape tweak) — see the open questionnaire for Yev's steer if he wants something else. *Done when:* the new behavior is observably distinct from the base enemy across several Survival runs and doesn't trivially end every run (playtested feel, not just code-correct), verified in browser. |
| p2-39 | Materials Run: smarter Auto Play pathfinding (A*/lookahead) | M | open | Follow-up to p2-25 (shipped 2026-07-11, `72cd18a`), from the Improvements inbox. `autoPlayBot()` (`gameplay.js`) is a single-step greedy scorer over a down-sampled grid (every 2nd tile) with no lookahead — sufficient for spectate/Watch Mode but visibly suboptimal (e.g. can dither near a danger-zone edge). Lower priority than the player-facing items above since this only affects the bot, not manual play. *Done when:* the bot survives materially longer / scores materially higher on average over N spectate runs than the current greedy version (agent picks and states the comparison method), verified in browser Watch Mode, `node --test tests/` still green. |
| p2-40 | River Run: hit/death model | M | open | From the 2026-07-22 modernization planning pass. Today any obstacle contact is instant game over — no lives, shield, or health bar. Whether to keep the one-hit-kill model (river-run's current identity — fast, tense, dodge-or-die) or add a lives/shield system is a real design fork, not an agent judgment call. Gated on Q1 of the new [River Run Questionnaire — Modernization Direction](questionnaires/river-run-modernization.md). |
| p2-41 | River Run: floating power-ups | M | open | From the 2026-07-22 modernization planning pass. No pickups exist today — only rocks/logs to dodge or shoot. Whether to add floating power-ups (shield, score multiplier, rapid-fire, speed control) and which ones is a real creative fork. Gated on Q2 of the new [River Run Questionnaire — Modernization Direction](questionnaires/river-run-modernization.md). |
| p2-42 | River Run: obstacle/enemy variety | M | open | From the 2026-07-22 modernization planning pass. Only two obstacle types exist (rock, log), both static floating hazards with identical straight-line drift, no motion relative to the boat. Whether to add moving obstacles, enemy boats that shoot back, or hazards tied to biome (p2-10) is a real creative fork. Gated on Q4 of the new [River Run Questionnaire — Modernization Direction](questionnaires/river-run-modernization.md). |
| p2-43 | River Run: near-miss scoring | S | open | From the 2026-07-22 modernization planning pass. p1-51 ships a visual/audio-only near-miss cue with no score change. Whether close dodges should also award bonus points (and how much) is a balance decision, not an agent judgment call. Gated on Q3 of the new [River Run Questionnaire — Modernization Direction](questionnaires/river-run-modernization.md). |
| p2-44 | River Run: mode structure | M | open | From the 2026-07-22 modernization planning pass. River Run has exactly one endless mode today. Whether to add mode variety (e.g. a Score Attack vs. Survival split like materials-run, or a no-fail "Zen" visualizer mode) or leave it single-mode is a real creative fork. Gated on Q5 of the new [River Run Questionnaire — Modernization Direction](questionnaires/river-run-modernization.md). |

---

## P3 — New Games

| ID | Item | Effort | Status | Notes |
|----|------|--------|--------|-------|
| p3-08 | New game jam — One-tower roguelike with hero sortie (B1+) | M | open | Questionnaire 2026-07-14, Q4=B1+: single central tower, waves converge from all sides, draft 1-of-3 upgrades between waves — plus a tappable **hero-sortie ability** (the Kingdom Rush itch as an ability, not a repositionable unit; growing it into B2 is a post-verdict decision). Meta-progression: **light unlocks** — new upgrade cards join the draft pool after milestones (not pure Balatro-clean runs). Between-wave drafting is the natural pause point. Design source: [New Game Directions](planning/new-game-directions.md) note §B1. |
| p3-09 | New game jam — Durak-deck score-attack roguelike (C1) | M | open | Questionnaire 2026-07-14, Q5=C1: "nostalgia-as-base is exactly the point." Durak's beat/transfer mechanic as Balatro's poker: clear escalating score quotas, buy folk-flavored modifiers between rounds (a домовой that doubles trump beats, a самовар that brews an extra card slot…). Hard constraint: NOT another durak variant — the durak-likes went to the Lab as genre mashes; this blends at the scoring layer only. Card rendering + deck logic already exist in the repo to borrow. Design source: [New Game Directions](planning/new-game-directions.md) note §C1. |
| p3-14 | Black Hole in One — Open World Sprint 3 (town & cosmetics) | M | open | Real town hub; **stardust → comet/trail skins** (cosmetics only, no power creep); beacons + fast-travel list; optional **boost/metered-travel** toggles (default stays unlimited free-fling). Per the Open World build plan. |
| p3-15 | Black Hole in One — Open World Sprint 4 (optional purpose) | M | open | Missions/bounties from town, portal-scroll item, richer discovery rewards — all ignorable. Per the Open World build plan. |
| p3-16 | Black Hole in One — 🧲 Orbits & Star Map arc | M | 🚧 in progress | Wave 1 (orbits attract + orbits do things): ORB-1 🧲 Orbit Magnet item ✅ shipped 2026-07-18 (`301effb`, v40) — orbit band captures on any approach, item ON by default, golf + item-OFF Explore untouched. Town Orbit (tee body) and tap-to-land ✅ shipped 2026-07-18. TAP-4 (tap the black hole you're orbiting to warp to Town) ✅ shipped 2026-07-18. ORB-2 (flick-at-planet guaranteed landing) superseded by the TAP redesign, not built as originally written. ORB-4 (stardust rings) ✅ shipped 2026-07-19 (`53784ba`) — Orbit Magnet capture snaps onto a ringed planet's ring so orbiting sweeps every dot; also fixed pickups always rendering green regardless of type. ORB-3 (station orbit refuel trickle + Town refuel) still open. Wave 2: MAP-1 (star-map landmarks) ✅ shipped 2026-07-19 (`368e161`), ahead of the Wave 1 checkpoint at Yev's call; MAP-2/MAP-3 (fast travel, pan/zoom) not started. Per the [Orbits & Star Map build plan](games/black-hole-in-one/plans/orbits-and-star-map.md). |
| p3-01 | Signal Tower | L | open | Listen to procedurally generated melody, reproduce on colored pads. Web Audio API + Canvas 2D. Mobile-first. |
| p3-02 | Hex Drift | L | open | Hex-grid territory game vs AI. CSS Grid + DOM. Turn-based strategy — fills a gap in the arcade lineup. |
| p3-03 | Glyph Run | L | open | Endless side-scroller: auto-run, swipe to jump/duck/dash, obstacles are glyphs matching a cognitive prompt. Canvas 2D. |
| p3-05 | Tysiacha: Hot-Seat Mode | L | open | Pass-and-play multiplayer for 2-3 local humans, using durak's pass-device cover pattern. **Confirmed via Q4 of the open-decisions questionnaire (2026-07-12):** build it, not just keep it on the roadmap. Still the biggest item on either game's roadmap (L); best suited to a dedicated session rather than a quick one. |

---

## Backlog — Tech Debt

Lower priority but worth doing when a game is getting other attention.

| ID | Item | Effort | Status | Notes |
|----|------|--------|--------|-------|
| b-01 | ES module split: river-run | L | open | index.html is 1024 lines with all JS inline (no `ui.js` — canvas/Three.js rendering is tightly coupled to state throughout, same call as durak-tactics/materials-run/blob-zapper: merge into `gameplay.js`). Split into constants/state/gameplay/main modules and add `tests/river-run.test.mjs` for the first time. **Sequenced first in the 2026-07-22 River Run modernization pass** — unblocks unit tests for p2-29/p1-05/p1-48…p1-51. |
| b-13 | Adopt an `AGENTS.md`-canonical context file | M | open | Make `AGENTS.md` the canonical agent context (read natively by more agent tools), with `CLAUDE.md` importing it and `GEMINI.md` generated from it, and generalize `scripts/generate-context-docs.js` accordingly. Today `CLAUDE.md` is canonical and `GEMINI.md` is generated (b-06). Do it as a deliberate pass — review the content line by line rather than regenerating blind. |
| b-29 | Rename durak `spendTokenAndStart()` → `startMatch()` | S | open | Cosmetic follow-up from the b-27 sweep (2026-07-13). The function no longer spends tokens (removed in Sprint 2) — it just starts a match. Live and correct, only the name is stale residue; called in 4 places in `games/durak/main.js`. Rename for clarity when durak is next touched. Not urgent — internal name, not user-visible. |

---
