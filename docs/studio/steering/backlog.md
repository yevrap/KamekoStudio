# Shadow Studio — Product Backlog

**Ordered: the top is next.** The row order is the priority; the `#` is the item's number,
kept so reviews and decision records can refer to it. This is the main way to steer
without writing a word. Move a row up to make it happen sooner, delete it to drop it, or
add a row anywhere in plain words; the Product Owner refines it at the next `plan`. Your ordering is kept unless a
retro writes down why not.

The Product Owner adds rows from the [Feedback Inbox](inbox.md), reviews, retros, the debt
register (`docs/studio/tech-debt.md`) and the games' idea files. It keeps the top five
**Ready** (sized, with acceptance criteria — `docs/studio/definition-of-ready.md`). The
epic and its budget are in [Direction](direction.md).

**Type:** feature · fix · test · docs · debt · process (the studio's own machinery — at most
one per sprint). **Size:** S or M; an L is split before it's pulled.

| # | Item | Type | Size | Source | Status |
|---|---|---|---|---|---|
| 39 | The executive's ADR-0011 commits pass the checks: add the full hash of each `docs(studio)` commit of 2026-09-23 with no ticket (*the studio runs itself* and *local first, Opus as the largest model*) to `COMMIT_EXEMPTIONS` with its reason, as [SHS-055](../iterations/05/tickets/SHS-055-checks-tell-studio-from-arcade.md) did for the 2026-09-22 ones. It is scoped `(studio)` with no ticket, so until this lands every `--stage=push` fails `commit-lint` (checked 2026-09-23) | process | S | Executive, ADR-0011 (2026-09-23). Outside the one-process-ticket cap: without it the gate can't pass | Ready. **Sprint 07's first build.** *Criteria:* `commit-lint` and `path-guard` report both commits as exempt with their reasons; a scratch-repo test shows an unexempted twin still fails |
| 40 | Branches and pull requests ([ADR-0011](../decisions/ADR-0011-the-studio-runs-itself.md) §4–5): the studio builds each ticket on `studio/SHS-NNN-slug`, opens a pull request with `gh`, the Independent Reviewer posts its verdict on it, CI (`.github/workflows/checks.yml`) runs, and the studio squash-merges and runs the post-deploy checks. Size L: split at plan (checks first, then the skill, then the first ticket through the new flow) | process | L | Executive, ADR-0011 (2026-09-23). Takes the sprint's process slot, one part per sprint, ahead of #21, so game work stays first | Needs refinement (split). *Criteria for the whole:* preflight and the `push` stage accept a clean `studio/*` branch as well as `main` (replacing `on-main`); a squash-merged commit whose subject names its ticket passes `commit-lint` and `path-guard`; `.claude/skills/studio-*/**` is a recorded exception in `rules.mjs` and the guardrails table; the `studio-iteration` skill's `build` and `review` steps describe the new flow; ADR-0007 is marked superseded; one real ticket has shipped through a merged pull request with CI green, as evidence |
| 33 | Production River Run: the same music-restart fix as #22, under ADR-0008 (production fix: the file listed, a regression test red then green, review before push). Higher stakes than first thought: the throw aborts `initGame` before the game loop, and since the sequence is never disposed every later restart throws too, so the river stays frozen until the page is reloaded (iteration 06 review: first freeze at restart 13, then 28 of 39). The arcade roadmap carries it as 🐞 p0-17; whichever lane ships first closes both | fix | S | Split from #22 at plan 06 · iteration 06 review, IR 1 | Ready; #22 has landed. First in sprint 07's plan unless the arcade lane has shipped p0-17 (still open at retro 06). Its review also reads `production-fix-reviewed`, whose last fix round (iteration 04) shipped unreviewed. *Criteria:* `games/river-run/index.html` restarts its music without `Sequence.stop()` (dispose, as the fork does) and a music failure can't stop the run starting; a browser test over 20 new runs sees every restart's game loop and music running and no uncaught rejection, plus a deterministic case (see #37) so the regression can't pass by luck; the entry in `PRODUCTION_FIXES` and the review record exist before the push |
| 37 | The fork's restart regression test ([SHS-061](../iterations/06/tickets/SHS-061-river-run-tone-start-time.md)) catches a reverted fix only by chance: against `musicSequence.stop()` put back it went red 6 of 7 runs (IR) and 4 of 6 (QA). Add a deterministic case | test | S | Iteration 06 review, IR 4 / QA F3 | Ready. *Criteria:* with `Tone.Sequence.prototype.stop` made to throw whenever the Transport is stopped, 20 restarts never reach it and every restart's loop runs; shown red against the reverted fix on 5 runs out of 5; the same shape is reused by #33 |
| 35 | River Run fork — on a phone the top controls (← Studio, Mute) sit over the score readout and hide it, and the power-up HUD covers the score from below. It dates from the fork's back link ([SHS-056](../iterations/05/tickets/SHS-056-river-run-fork.md)) | fix | S | [SHS-060](../iterations/06/tickets/SHS-060-river-run-power-ups.md) build, phone screenshots · iteration 06 review, IR 2 / QA F2 | Ready; with #38, the team's Iterate on the power-ups. *Criteria:* at 390×780 and 320×640 the score and the power-up HUD are fully visible, overlap no control and not each other (iteration 06 review: at 320 and 375 wide the score wraps to two lines and the HUD covers its number); the back link and mute keep 44px targets; a browser test compares their bounding boxes |
| 21 | An executive edit to `docs/studio/steering/` (an inbox line, a questionnaire answer, a backlog move) has a commit form the checks accept, recorded in `process.md` and the steering README's *How to steer*. Today it is either an arcade commit touching a studio path or an unnumbered `(studio)` commit, and both fail. *Criteria:* a `docs(studio): steer: …` commit with no ticket, changing only `docs/studio/steering/**`, passes `commit-lint` and `path-guard` and is reported as *steering*; the same form changing any other path fails; an unnumbered `(studio)` commit of any other shape still fails; each shown red first on a scratch repository | process | S | [SHS-055](../iterations/05/tickets/SHS-055-checks-tell-studio-from-arcade.md) build, iteration 05 · retro 05: the main way to steer shouldn't fail a check | Ready. Held out of sprint 06: the executive's focus (#32) took the sprint's one process slot. Sprint 07's process slot |
| 8 | River Run fork — the game-over screen shows the best score, with a "New Best" mark | feature | S | Arcade roadmap p1-48, now the fork's | Ready (#2 landed in sprint 05). *Criteria:* game over shows the fork's best score from `studio_riverRun_highScore`, and "New Best" only when this run beat it; a browser test covers both |
| 31 | River Run fork — a player who came in through the 3D portal has a way back to the 3D page, not only "← Studio" | feature | S | Iteration 05 review, QA F6 | Ready. *Criteria:* opened from the 3D page's portal, the fork shows a way back to `3d.html` as well as "← Studio"; opened from the realm, only "← Studio"; a browser test covers both; the fork's edit list names the change |
| 38 | River Run fork — the power-up timers run on real time, not frames: on a 120 Hz phone the spread shot's "6.0s" lasts 3 s today, because the whole game counts frames. The HUD's seconds and the power-up's length both come from elapsed time; the drawer's pause still pauses them | fix | S | Iteration 06 review, QA F5 · retro 06 (Active rule 9) · the team's Iterate on [SHS-060](../iterations/06/tickets/SHS-060-river-run-power-ups.md) | Ready, **unless the executive's verdict on the power-ups is Kill** (then Won't do). *Criteria:* with the frame rate doubled in a browser test (two `update` steps per animation frame, or a 120 Hz clock stub), the spread shot ends within 6 ± 0.3 s of wall time and the HUD's countdown matches it; with the drawer open, the timer doesn't move; the existing power-up subtests stay green |
| 36 | The ticket-link script writes a broken link into Markdown shapes it misreads: link text wrapped across lines, a shortcut reference `[SHS-NNN]`, a reference definition's title, a 4-space indented code block, an HTML attribute, CRLF frontmatter, a title line after frontmatter. None is in the repo today; `_SHS-NNN_` is left plain under the wrong reason. Either skip these shapes or make the retro's run fail loudly on them | process | S | Iteration 06 review, IR 3 / QA F4 | Needs refinement: cheap guard vs. real parsing. The lib's header names the first four since `c361b17` |
| 10 | A real repository README (production documentation, so it goes through the production-fix rule) | docs | S | Inbox 2026-09-21 audit — carried four times (retro 06). Retro 05: the first production fix since iteration 04, so its review also reads `production-fix-reviewed`, whose last fix round shipped unreviewed | Ready |
| 14 | Ratify the move to the repo as the studio's single home with a decision record; fix `public-repo-hygiene.md`, which still says direction is kept outside the repository | docs | S | Inbox 2026-09-22 | Ready |
| 11 | Tighten the TD-009 direct test: refuse a fix that keeps only one burst particle | test | S | QA, iteration 04 review round 2 | Ready |
| 12 | TD-013: `path-guard` refuses a fix file changed by a merge it can't see | debt | S | Tech-debt register | Ready |
| 16 | Overtighten's torque readout rounds while the state doesn't: 45.6 shows "46 · loose" | fix | S | Board, noticed in iteration 04 | Ready |
| 6 | River Run fork — a near-miss streak: tight dodges build a streak the HUD shows, a hit or a wide pass resets it | feature | M | River Run questionnaire Q3=C · arcade p2-43, and p1-51's near-miss cue (whoosh + edge flash) as its first half | Needs refinement: Q12 took power-ups first; the next experiment waits on their verdict |
| 7 | River Run fork — biomes that change play: the stretch of river changes how the boat handles (friction) and what floats in it, and the change is noticeable | feature | M | River Run questionnaire Q6=B, Q4=A/C · arcade p2-10, p2-42 | Needs refinement: Q12 took power-ups first; the next experiment waits on their verdict |
| 24 | River Run fork — impact feel: a fatal hit shakes the camera, flashes red and plays a synthesized crash; a shot obstacle plays a short destroy sound; both respect mute | feature | S | Arcade roadmap p1-49 + p1-50, now the fork's | Needs refinement |
| 23 | River Run fork — ghost run: record the best run's boat positions and replay them as a transparent ghost on the next attempt | feature | M | Arcade roadmap p1-05, now the fork's | Needs refinement |
| 25 | River Run fork — a livelier Watch Mode: the auto boat carves across the whole river and weaves between obstacles instead of hugging an edge, surviving no worse than today | feature | M | Arcade roadmap p2-29, now the fork's | Needs refinement |
| 5 | River Run fork — split the inline script into ES modules (constants/state/gameplay/main) and add its first unit tests, so experiments are testable. Size L: split per module at plan | debt | L | Arcade roadmap b-01, now the fork's | Needs refinement |
| 26 | The 3D page and the drawer know River Run has two builds: the portal's trophy reads the fork's `studio_riverRun_highScore` (or both), and the drawer's game switcher on the fork page stays in the fork. Production code (`shared/3d/gameplay.js`, `shared/settings.js`), so it needs an approved exception or a production fix | feature | M | Iteration 05 review, IR findings 2 and 4, QA F4 · ADR-0010 | Needs refinement: stop-and-ask, touches production |
| 27 | Pin `STUDIO_FORK_PORTALS` (and `PATH_EXCEPTIONS`) to the decision records that approve each row, so a studio commit can't approve its own portal | test | S | Iteration 05 review, IR finding 5 | Needs refinement |
| 28 | Enforce the arcade-docs route: an arcade commit made by a studio session may change only doc paths (`CLAUDE.md`, `GEMINI.md`, `games/CLAUDE.md`, `docs/roadmap.md`, `docs/archive/**`, `docs/games/**`, `docs/questionnaires/**`). Today it is discipline, not a check | process | M | Iteration 05 review, IR finding 1 (the M half) | Needs refinement |
| 29 | A recorded exception judges only what studio commits changed in its file, so a legitimate arcade edit to `shared/3d/constants.js` (a new game's portal) in the same range stops failing the path guard | process | S | Iteration 05 review, QA F2 | Needs refinement |
| 30 | The path guard marks working-tree paths "(uncommitted)" in its output, so an arcade edit in progress isn't read as a studio commit | process | S | Iteration 05 review, QA F3 | Needs refinement |
| 9 | Let the studio add tests and docs for production games: widen the path guard to test and doc paths only, never game code, with a decision record | process | S | Executive, 2026-09-22 — tests, docs and debt are welcome work | Needs refinement |
| 34 | Link the older `SS-NNN` ticket mentions to their files too, with the same script | docs | S | Plan 06, left out of #32's scope, which names `SHS` | Needs refinement: the executive asked for `SHS` mentions; widen only if wanted |
| 15 | Decide whether `studio:check`'s `hygiene` stays studio-scoped or defers to the repo-wide scan in `npm test` | debt | S | Inbox 2026-09-22 | Needs refinement |
| 17 | TD-005: with site data blocked, `shared/settings.js` throws on every page (production fix) | debt | S | Tech-debt register | Needs refinement |
| 18 | TD-006: the 3D page's interaction prompt always prefers a portal to a nearby trophy | debt | S | Tech-debt register | Needs refinement |
| 19 | TD-007: the studio's browser harness duplicates `scripts/smoke.mjs`'s static server | debt | S | Tech-debt register | Needs refinement |
| 20 | Maze Warden fork — calm the board, then one real choice per tower under a cap on effects. Held: E1's first fork went to River Run, so Maze Warden's Iteration 8 stays on the arcade roadmap unless a later fork takes it | feature | M | Former #4 and #5, Q11 ⭐ not taken | Parked |

*Done and removed:* #1, #2, #3 (sprint 05: [SHS-055](../iterations/05/tickets/SHS-055-checks-tell-studio-from-arcade.md), [SHS-056](../iterations/05/tickets/SHS-056-river-run-fork.md), [SHS-057](../iterations/05/tickets/SHS-057-portal-opens-fork.md)); #13 (retro 05: the
Active rules list in `learning-log.md`); #32, #4, #22 (sprint 06: [SHS-059](../iterations/06/tickets/SHS-059-ticket-mentions-link-to-tickets.md), [SHS-060](../iterations/06/tickets/SHS-060-river-run-power-ups.md),
[SHS-061](../iterations/06/tickets/SHS-061-river-run-tone-start-time.md)). The next new item is #41.

---

*Related: [Direction](direction.md) · [Next step](next.md) · [Board](board.md) · [Feedback Inbox](inbox.md)*
