# Shadow Studio — Next step

**Next:** `plan 10` — sprint 10, E2's last granted sprint: a second pass on Samovar's core
**Say:** "run the studio" (the whole sprint, watched), or `studio next` (this one step, in a new session)

| | |
|---|---|
| Epic | E2 · The studio's first original game worth playing · sprint 3 of 3 (+1 reserve, unclaimed) |
| Sprint | 10 · to be planned (sprint 09 closed, [retro](../iterations/09/retro.md) done) |
| Steps | plan · build … · review · close · retro |
| Waiting on you | Nothing blocks. Q18 takes its ⭐ at plan (a flow per guest); Q17 is in the skill; Q16 waits for your tick and blocks nothing |

## Notes for the next session

- Top five Ready: #57 (Samovar's flow per guest, Q18 ⭐), #58 (the glasses drawn true), #50
  (the first real pull request, riding on the first games ticket), #47 (`docs-current` at
  push, the process slot, ahead of #21), #60 (a hung suite fails by name, TD-015).
- The pull-request flow starts here (skill `build` step 2). A production fix still stays on
  trunk until #56.
- A bypass the numbers show is decided at plan (the ticket template, retro 09): #57's
  flow range has to beat a scripted player who counts, on paper before the build.
- Retro 09's change is to QA, which now tests the tests. Retro 10 checks QA's cost ($1.93
  in 09) and whether it finds anything the Independent Reviewer doesn't. Retro 10 also
  writes E2's epic review: E3 on a Keep, or the reserve decision on an Iterate.
- The next new backlog item is #61, the next ticket SHS-076.

*Rewritten by every session (the studio-iteration skill). Loaded into every new Claude Code
session by the SessionStart hook in `.claude/settings.json`.*
