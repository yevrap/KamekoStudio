# Shadow Studio — Next step

**Next:** `restart` — the studio is tabled (since 2026-09-25, after sprint 09's retro)
**Say:** "restart the studio" when you want it back (optionally "… — focus: X"); until then nothing runs

| | |
|---|---|
| Status | Tabled by the executive ([SHS-076](../iterations/09/tickets/SHS-076-table-the-studio.md)). Nothing in flight: no open pull request, studio branch or unpushed fix |
| Epic | E2 · paused at sprint 2 of 3 (+1 reserve, unclaimed); the budget clock is stopped |
| The restart | Reads [restart.md](restart.md) (where it stopped, what sprints 00–09 taught, the executive's feedback, the capability baseline) and has two scouts check what's new in AI tools and practice. It changes the process, picks the epic, and stops before plan 10 |
| Waiting on you | Nothing. Q16 still waits for your tick; Q18's ⭐ would be taken at plan 10 |

## Notes for the restart

- Sprint 10 as it would have been planned: #57 (Samovar's flow per guest), #58 (the glasses
  drawn true), #50 (the first real pull request), #47 (`docs-current` at push). The
  feedback (P1, P2) may reorder or drop them.
- `studio` issues filed while tabled wait for the restart, which reads them first.
- The next new backlog item is #61, the next ticket SHS-077.

*Rewritten by every session (the studio-iteration skill). Loaded into every new Claude Code
session by the SessionStart hook in `.claude/settings.json`; while tabled, the hook shows
only the lines above the table.*
