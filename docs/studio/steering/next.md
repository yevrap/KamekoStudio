# Shadow Studio — Next step

**Next:** `plan` sprint 05, the first sprint of epic E1
**Say:** `studio next` (in a new session) · or `studio next — focus: <what you want>`

| | |
|---|---|
| Epic | E1 · The Studio Wing opens · sprint 1 of 3 (+1 reserve) |
| Sprint | 05 · not planned yet |
| Steps | **plan ▶** · build · review · close · retro |
| Waiting on you | Nothing blocks. Q11 (first fork) takes the ⭐ Maze Warden if blank |

## Notes for the next session

- First run under ADR-0009 (one step per session). Read `direction.md`, then
  `backlog.md`; the inbox's 2026-09-22 lines are already rows in the backlog, so log
  them in the input ledger and clear them.
- Backlog #1 (the shared-`main` blocker) must land before any other ticket pushes: the
  `push` stage's `path-guard` and `commit-lint` will read arcade commits as violations.
- Last tag: `studio-iteration-04`. Last ticket: SHS-054, so sprint 05 starts at SHS-055.

*Rewritten by every session (the studio-iteration skill). Loaded into every new Claude Code
session by the SessionStart hook in `.claude/settings.json`.*
