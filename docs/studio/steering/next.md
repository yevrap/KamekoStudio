# Shadow Studio — Next step

**Next:** `plan 09` — act on Samovar's Iterate (#51, #52 with #53 on Q15's ⭐, #54), with #49 in the process slot
**Say:** "run the studio" (the whole sprint, watched), or `studio next` (this one step, in a new session)

| | |
|---|---|
| Epic | E2 · The studio's first original game worth playing · sprint 2 of 3 (+1 reserve, unclaimed) |
| Sprint | 09 · not planned yet |
| Steps | plan · build · review · close · retro |
| Waiting on you | Nothing blocks. Q15 takes its ⭐ (cup shapes) at plan unless ticked; Q16 (trim the root `CLAUDE.md`) waits for your tick and blocks nothing |

## Notes for the next session

- Retro 08 is done ([retro.md](../iterations/08/retro.md)); issues #3 and #4 are closed.
  Sprint 08 cost $18.37 from plan to close ($17.98 in 07); review is 43% of it.
- The retro's one change: the Playtester keeps screenshots out of its context. Retro 09
  checks review's writes ($3.43) and the Playtester's writes and reads ($1.97) with
  `node tests/studio/sprint-cost.mjs`, and prices output from the workflow's tokens.
- A Samovar ticket that changes the core decision states its numbers per cup before the
  build (`templates/ticket.md`, retro 08). #53's criteria already carry them.
- The next new backlog item is #56, the next ticket SHS-072.

*Rewritten by every session (the studio-iteration skill). Loaded into every new Claude Code
session by the SessionStart hook in `.claude/settings.json`.*
