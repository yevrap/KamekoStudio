# SHS-052 — pre-push review

The independent review of a production fix, recorded before the fix is pushed (ADR-0008).
The `push` and `gate` stages refuse the fix unless every commit that changed its production
files is contained in the commit named here.

- **Reviewed:** 7b8a0febeab25b7af95b7186d0b3e5d3af4dd2c6
- **Verdict:** APPROVED WITH FINDINGS

## Who reviewed it

Iteration 04's first review round. Two passes, each with fresh context and neither by the
author:

- the Independent Reviewer, on a different model from the author;
- QA, on the author's model.

Both read `studio-iteration-03..7b8a0fe`. That range contains SHS-052's only commit to
production files so far, `fb8c144`.

## Recorded after the fact

**SHS-052 was pushed, and so live, before any review saw it.** The rule this record
satisfies did not exist yet: it is SHS-054, opened by this same review round. So this
record is written from the round's two reports. It does not come from a review held before
the push.

Neither pass gave SHS-052 a verdict of its own. Each gave one for the whole iteration, and
both rejected it, for SHS-050 and SHS-051. The verdict above is the team's reading of what
each pass said about SHS-052:

- **The Independent Reviewer** found the fix "correct and minimal", running before the loop
  that dereferences the black hole, and confirmed that the live site serves it. It found
  the three new tests "deterministic", each proving its precondition, with the direct test
  "genuinely" telling a real fix from partial ones.
- **QA** found "the TD-009 fix is sound: verified on all four player routes, red without
  the fix, green with it, and golf unchanged". As a player, 20 of 20 trials threw on the
  unfixed build and 0 of 20 on the fixed one.

QA's two findings on SHS-052 are what "with findings" refers to:

- the direct test accepts an over-broad fix, one that also drops the Explore bursts;
- the fix line allocates a new array on every Explore frame.

Both are fixed in SHS-052's fix round 1. That round changes the ticket's production files
again, so this record stops covering them. The `push` stage refuses that change until the
second review round has seen it and this record names the commit it saw.
