# SHS-066 — pre-push review

The independent review of a production fix, recorded before the fix is pushed (ADR-0008).
The `push` and `gate` stages refuse the fix unless every file it owns is, at `HEAD`,
exactly what the commit named here holds.

- **Reviewed:** 1414dabd101120d34dfe685a622030790a86ea87
- **Verdict:** APPROVED WITH FINDINGS

## Who reviewed it

Sprint 07's one review round, run by the `studio-sprint` workflow (ADR-0011). Two passes,
each with fresh context and neither by the author:

- the Independent Reviewer (`opus`), on the fix commit `1414dab` and the arcade `docs:`
  commit `61e69c6` that marks p0-17 done;
- QA (`sonnet`), on the whole sprint including [SHS-066](../tickets/SHS-066-production-river-run-restart.md).

The Playtester also played the arcade's River Run from the gallery link and gave it a
**Keep** (see [the review](../review.md)).

Each pass's own words:

- **Independent Reviewer — "Approve with findings."** "I reviewed the production fix at
  1414dabd… and it is correct and minimal: on a scratch copy its deterministic tests were
  red 3 of 3 against the unfixed file, and it held 80 of 80 restarts at HEAD." Beyond the
  ticket: 40 restarts in each of two fresh browser contexts on both `/games/river-run/`
  and `/studio/games/river-run/` at `HEAD`, none froze, no page errors; `npm run e2e`
  29/29; `npm test` 840/840.
- **QA — "Approve with findings."** "Code, tests and process claims for [SHS-064](../tickets/SHS-064-river-run-power-up-hud-real-time.md)/065/066
  all verified against a real local run and independent reproduction."

The verdict above is the lower of the two; both are the same.

## Findings on this fix, and what became of them

| # | Finding | Became |
|---|---|---|
| IR-1 · QA F1 | The ticket was Done with its first criterion unticked, though the code meets it, and kept the template's placeholder line above a filled-in Result | **Fixed now** (docs, this review's record commit): criteria 1 and 5 ticked, the placeholder removed |
| IR-3 | The fix ships without a `version.json` bump, so a player with the arcade already open isn't offered the "New version available" banner. A studio commit can't make the bump: `version.json` is neither an allowed path nor in `PRODUCTION_FIXES`. The same gap existed for [SHS-052](../../04/tickets/SHS-052-td-009-fixed.md). Low impact, since there is no service worker | **Backlog #43**, with a question in the questionnaire (Q13, ⭐ every production fix bumps `version.json` in its fix commit). It touches the arcade's release marker, so the team does not bump it on its own |

Neither finding changes `games/river-run/index.html` or `scripts/e2e.mjs`: at `HEAD`, both
files are byte for byte what `1414dab` holds.
