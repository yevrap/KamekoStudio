# Tech debt

## Policy

- Debt is **taken deliberately or not at all.** A shortcut that is worth it gets a row
  below, in the same iteration that takes it. A shortcut nobody is willing to write down
  is not worth taking.
- Every row names the **cost of leaving it** — what gets harder, or what will break.
- Roughly **20% of each iteration** is reserved for debt, refactoring, docs and learning.
  The retro checks that the reservation was actually used.
- A row is closed by a ticket, and the closing ticket ID is recorded.
- Debt is not a wish list. "It would be nicer as X" belongs in the backlog, not here.

## Register

| ID | Opened | Description | Cost of leaving it | Size | Status |
|---|---|---|---|---|---|
| TD-001 | 00 | The realm has no entrance. It is reachable only by typing the URL. | The realm is invisible to anyone who does not already know it exists. | S | Open — **held by decision**, not by capacity: the door waits until there is a gallery worth entering. The twelfth portal slot, the centre of the front wall facing the spawn point, is free and reserved for it. Filling it will mask the middle trophy's prompt until TD-006 is resolved. ADR-0005 |
| TD-002 | 00 | The 3D landing page could only show 9 portals while `ARCADE_GAMES` listed 11, so the last two entries were silently dropped by `if (!positions[index]) return;`. | Two shipped games (Black Hole in One, Maze Warden) had no portal, and nobody noticed. | M | **Closed** in 01 by SS-014: a front-wall row takes the room to 12 slots, and the `portal-capacity` check now counts the list against the table so the same defect cannot recur unreported. ADR-0005 |
| TD-003 | 00 | `tests/guard-localStorage.test.mjs` scans `shared/` and `games/` only, so studio storage keys are checked by the studio's own guard rather than by the repo-wide one. Two guards, one rule. | The rule can drift between the two implementations. | S | Open — accepted for now; see `decisions/ADR-0003-storage-namespace.md` |
| TD-005 | 00 | With site data blocked, `shared/settings.js` throws an uncaught `SecurityError` from `getSavedTheme` on every page that loads it, studio pages included. The studio's own code handles blocked storage correctly; this throw is production's, and `shared/settings.js` is outside the path guard. | A studio page reports an uncaught error in a configuration the studio cannot fix. Measured in 01: the throw also stops `body.dark-mode` ever being applied, so a reader with site data blocked and a dark theme gets the light page on every page of the arcade. It cost iteration 01 a falsely-ticked acceptance criterion. | S | Open — production-side; needs an exception or a production ticket |
| TD-004 | 00 | `npm run smoke` discovers pages under `games/` and `drafts/` only, so `studio/` pages are in no boot suite. `studio/index.html` and `studio/style.css` have no automated coverage at all. | Measured in 01 by an independent QA pass: eight mutations — deleting the `main.js` script tag, the shelf container, the back link or the `<noscript>` block; moving both breakpoints; zeroing every `min-height`; emptying the killed-card rule; removing the storage try/catch — all leave the entire suite green. Four of SS-013's criteria rest on a human having looked once. | M | **Closed** in 02 by SS-020: `studio-boot` opens every page found under `studio/**` in a real Chrome and holds it to a contract, in three configurations — ordinary, scripting disabled, and site data blocked. All eight mutations were demonstrated failing it, each with a distinct message, and the demonstrations are recorded on the ticket. Pages are discovered rather than listed, so the next page added arrives covered |
| TD-006 | 01 | `updatePlayer()` in `shared/3d/gameplay.js` chooses the interaction prompt by 2D distance and prefers a portal to a trophy unconditionally — `if (closestPortal) … else if (closestTrophy)` — so a nearer trophy loses to a further portal. | Any portal placed within interaction range of the trophy shelf silently replaces every trophy description there. It cost this iteration a ticket to route around, and it blocks the reserved twelfth slot. | S | Open — production-side; needs an exception or a production ticket. Routed around by SS-018 |
| TD-007 | 02 | `tests/studio/lib/browser.mjs` stands up a static file server and launches Chrome, which is what `scripts/smoke.mjs` already does for production. Two servers, one job. | The two can drift — a MIME type, a path-traversal guard or a settle time fixed in one and not the other. Small today: the studio's copy serves six extensions and one directory tree. | S | Open — accepted deliberately. `scripts/` is outside the path guard and `smoke.mjs` exports nothing, so the alternatives were a production exception for test plumbing or this. SS-020 chose this, on the instruction that the boot check be studio-owned |
| TD-008 | 02 | `studio-boot` covers what it collects, and four things it does not collect are written down in `self-checks.md`: no audio observation, no layout or visual regression beyond one bolt's pixels, only the first plate is played so the all-cleared ending is never reached, and the realm home is measured rather than driven. | Each is a class of defect that ships green. Ten rounds of review found 4, then 7, then 10, then 12, then 6, then 4, then 9, then 4, then 8, then 1 further mutations the check survived; the surface is unbounded, and a gap described in prose with no row schedules nothing. | M | Open — accepted deliberately. The alternative is an open-ended chase; the policy the studio settled on instead is that a new rule requires a new observation, and that the limits are stated rather than implied |
| TD-009 | 02 | Black Hole in One: entering **Explore** while a golf hole's spiral particles are alive throws `Cannot read properties of null (reading 'x')` from `stepParticles` (`games/black-hole-in-one/ui.js:171`) on every frame for about a second. **Diagnosed in 03 by SHS-044.** `stepParticles` moves every spiral particle around `world.blackHole`; Explore's world reset (`resetWorld` in `explore.js`) sets `world.blackHole = null` and leaves the particles; and spirals spawn around any golf hole's black hole — including the decorative one drawn behind the start menu. Two end-to-end tests start Explore within a few frames of load and so fail only when a spiral happened to spawn in time: `🔄 New Map regenerates the Explore world…` (the one seen failing, once in three consecutive suite runs at the iteration-02 deploy) and `running out of fuel mid-flight… (FUEL-3)`. New Map and fuel have nothing to do with it. Three suite runs were too few to establish a rate, and no rate is claimed here: an isolated replica of each test, run by the independent QA pass outside the repository, failed a few times in 150 — enough to show the race is real, not to say how often the suite hits it. | **A player reaches it** by every route from a black hole to the Explore button that has been tried: the start menu of a fresh profile; a golf round left through ☰ Menu; a golf round left through ⚙️ and the Play tab; and a shared map left through its own ☰ Menu. Each threw in every forced trial, and the first two in every unforced one (`--as-a-player`). Each throw aborts the frame before `ui.render`, so the canvas stalls for about a second. It also makes the studio's "whole suite is green before merging" guarantee unreliable for a reason the studio did not cause, and hides any real regression on those paths. | S | Open — **production-side; the fix is specified, not made.** Check with `node tests/studio/diagnostics/td-009.mjs`: it tries those four routes, a control with no black hole, and the defect directly — in a golf round, spirals **proven** alive by counting reads of the black hole's position, then the black hole removed and one step taken. It exits 1 while any route throws, 0 only when every route **and** the direct check are clean, 2 for a result that does not fit this diagnosis — a fix that clears the routes but not the defect, or spirals that cannot be created at all — and 4 if the script itself breaks. **The fix** is one line in `stepParticles`, dropping spiral particles when there is no black hole (`if (!bh) particles = particles.filter(p => !p.spiral);`), plus a deterministic end-to-end regression test for both routes. Tried on scratch copies of the repository only: with it the check exits 0 and `npm run e2e` passes 23/23. Six wrong or partial fixes were tried against it the same way, and none is reported fixed — stopping spirals on the menu, drawing no golf hole behind the menu, or stopping them while any menu shows and clearing them on ☰ Menu all exit 1; clearing particles on entering Explore, clearing them whenever a menu opens, or removing spirals altogether exit 2. Two production files, `ui.js` and `scripts/e2e.mjs`, so it needs the executive's approval — as a production ticket, or as a one-time exception |
| TD-010 | 03 | `studio-live` waits for the studio page to return 200 and then searches for the deploy marker **once**. The page returns 200 before and after a deploy, so on a fresh push it checks the old build immediately and fails — the polling it documents only ever applies to the status code. Found by iteration 03's publish step: *"marker … not found … after 1 attempt(s)"*, then a pass once Pages had deployed. It arrived with SS-040, which rewrote the marker search after iteration 02's tag and was never reviewed. | A correct deploy reads as a failure unless someone waits and re-runs by hand; under trunk-based work, with a post-deploy check on every ticket's push, that happens every time. | S | **Closed** in 04 by SHS-050: `studio-live` searches afresh on every attempt and reports which attempt found the build. It showed the defect once more on iteration 04's first push, eight seconds after it; the fixed check is held by `tests/studio/deploy.test.mjs` |
| TD-011 | 03 | `production-unchanged` compares against `--previous-tag`, or else the newest `studio-iteration-*` tag. Run after the iteration's own tag is pushed — which is when the protocol runs it — the newest tag is the iteration itself, so it compares the release with itself and passes having proved nothing: *"nothing outside the guard changed since studio-iteration-03 (0 path(s) inside the guard)"*. Iteration 03's publish step caught it from that zero and re-ran with `--previous-tag=studio-iteration-02`. | The one check that proves production was not touched can pass vacuously, and says so only in a number nobody is required to read. | S | **Closed** in 04 by SHS-050: `--previous-tag` and `--base` default to the newest iteration tag that is not `HEAD`, and a comparison covering no commits reports `not run` instead of `pass`. Held by `tests/studio/baseline.test.mjs` |
